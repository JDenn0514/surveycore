# Implementation: nonprob-bootstrap-variance PR 1

## Write Surface

**Modified:**
- `plans/error-messages.md` — added NB-1 through NB-8 rows, updated trigger descriptions for rows 16/17
- `tests/testthat/helper-test-data.R` — extended `test_invariants()` for `survey_nonprob`: checks 5 repweight keys
- `tests/testthat/test-constructors.R` — added `as_survey_nonprob() — repweights and bootstrap arguments` section (14 new test blocks)
- `tests/testthat/test-validators.R` — added 3 new `.validate_rscales()` test blocks (NA via nonprob, negative via nonprob, NA via replicate)
- `tests/testthat/test-s7-classes.R` — updated 3 direct `survey_nonprob()` calls that call `test_invariants()` to include all 5 repweight keys
- `R/core-validators.R` — extended `.validate_rscales()` to check NA/non-numeric/negative values (new error class `surveycore_error_rscales_na`)
- `R/core-classes.R` — added `reference_sample` property to `survey_nonprob`; updated roxygen `@usage` and `@param` for new property and `@variables` section
- `R/core-constructors.R` — added 6 new arguments to `as_survey_nonprob()`; full resolution + validation logic; updated roxygen
- `man/as_survey_nonprob.Rd` — regenerated by `devtools::document()`
- `man/survey_nonprob.Rd` — regenerated by `devtools::document()`

## Summary

- Extended `as_survey_nonprob()` with 6 new arguments: `repweights`, `type`, `scale`, `rscales`, `mse`, `reference_sample`. Full resolution and validation logic follows the spec's ordered check sequence.
- Added `reference_sample` property to `survey_nonprob` S7 class (Layer 3 type-check in constructor, no S7 validator change).
- Extended `.validate_rscales()` to catch NA, non-numeric, and negative values with typed error class `surveycore_error_rscales_na`; fires from both `as_survey_nonprob()` and `as_survey_replicate()` paths.
- `@variables` for `survey_nonprob` now always contains all 5 repweight keys (`repweights`, `type`, `scale`, `rscales`, `mse`); NULL when no repweights, non-NULL when repweights present.
- `test_invariants()` extended: for `survey_nonprob` objects, asserts all 5 repweight keys are present in `@variables`.

## Task Checklist

- [x] `plans/error-messages.md` updated — NB-1 through NB-8, updated rows 16/17 trigger descriptions
- [x] `test_invariants()` extended for `survey_nonprob` — asserts all 5 repweight keys present
- [x] Constructor tests written — 14 new `test_that()` blocks (happy path + error paths)
- [x] Validator tests written — 3 new blocks for `surveycore_error_rscales_na`
- [x] Tests confirmed red before implementation
- [x] `.validate_rscales()` extended — NA/non-numeric/negative detection
- [x] `reference_sample` property added to `survey_nonprob` class
- [x] `as_survey_nonprob()` extended with full resolution + validation logic
- [x] Tests confirmed green after implementation
- [x] `devtools::document()` run
- [x] `devtools::check()` — 0 errors, 1 pre-existing warning (`surveytidy` unstated dependency), 2 pre-approved notes

## Notes for Tester

- The pre-existing `checking for unstated dependencies in 'tests' ... WARNING '::' or ':::' import not declared from: 'surveytidy'` is documented as acceptable in the task spec and predates this PR.
- Three tests in `test-s7-classes.R` that call `test_invariants()` on direct `survey_nonprob()` constructions were updated to include all 5 repweight keys (as NULL) — this is a necessary side-effect of the `test_invariants()` extension.
- `survey_nonprob` now requires `as_survey_nonprob()` (not direct construction) to produce correct `@variables` structure unless all 5 repweight keys are manually specified.
