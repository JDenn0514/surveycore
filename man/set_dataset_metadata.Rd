% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-metadata.R
\name{set_dataset_metadata}
\alias{set_dataset_metadata}
\title{Set Dataset-Level Metadata}
\usage{
set_dataset_metadata(x, ..., key = NULL, value = NULL)
}
\arguments{
\item{x}{A survey design object or \code{data.frame}.}

\item{...}{Named arguments where the name is a dataset metadata key and the
value is the value to store. A single named list is also accepted.
Supports \verb{!!!} list splicing. A \code{NULL} value deletes the key.}

\item{key}{\code{character} vector of key names, or \code{NULL} (default). Use with
\code{value}. Must be supplied by name, because it follows \code{...}.}

\item{value}{A list with one element per element of \code{key}, or \code{NULL}
(default). An atomic vector is coerced with \code{as.list()}. Names on \code{value}
are ignored — \code{key} supplies the names. A \code{NULL} element deletes that one
key; \code{value = NULL} deletes every key in \code{key} and skips the length check.
Must be supplied by name, because it follows \code{...}.}
}
\value{
The modified object, invisibly.
}
\description{
Sets whole-dataset metadata — the survey name, the display name, the fielding
vendor, and the field dates — on a survey design object or a data frame,
using one of three conventions.
}
\details{
\strong{Convention 1 (named \code{...})} — recommended:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{set_dataset_metadata(x, vendor = "Ipsos", field_start = "2026-02-10")
}\if{html}{\out{</div>}}

\strong{Convention 2 (single named list in \code{...})}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{set_dataset_metadata(x, list(vendor = "Ipsos"))
}\if{html}{\out{</div>}}

\strong{Convention 3 (\code{key} + \code{value})}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{set_dataset_metadata(x, key = "vendor", value = "Ipsos")
}\if{html}{\out{</div>}}

The key vocabulary is closed. Exactly six keys are valid, and this is their
canonical order:

\describe{
\item{\code{survey_name}}{\code{character(1)}. Full formal survey name.}
\item{\code{data_name}}{\code{character(1)}. Display label for this dataset.}
\item{\code{vendor}}{\code{character(1)}. Fielding vendor.}
\item{\code{field_start}}{\code{Date(1)}. First day in the field.}
\item{\code{field_end}}{\code{Date(1)}. Last day in the field.}
\item{\code{field_period}}{\code{character(1)}. Prose field period, for display.}
}

Any other key is an error. The four character keys take one non-\code{NA} string.
The two date keys take a \code{Date} scalar or an ISO 8601 string
(\code{"YYYY-MM-DD"}), which is stored as a \code{Date}; a looser string such as
\code{"2026/02/10"} or \code{"2026-2-1"} is rejected. Stored keys are always kept in
the canonical order above.

\code{survey_name} and \code{data_name} are independent. This function never reads one
to fill or check the other.

A \code{NULL} value deletes a key. Deleting a key that is not set does nothing and
raises no condition. A zero-length value such as \code{character(0)} is not a
deletion — it is an invalid value.

\code{field_start} must not be after \code{field_end}. The comparison uses the
\emph{effective} value of each date: the new value when the call supplies one, no
value when the call supplies \code{NULL}, and the stored value otherwise. So one
call can repair a reversed stored pair by deleting one date and setting the
other. Every check runs before any write, so a rejected call changes nothing.

On a survey design object the values go into the design's metadata, which is
the single source of truth on every read path; attributes on the underlying
data frame are neither read nor written. On a data frame the values go into
whole-object attributes, one per key, and the columns are left untouched —
even a column whose name matches a key.

\code{dates} is the pre-1.2.0 attribute name for the prose field period. Setting
it is an error that points at \code{field_period}. Deleting it is allowed:
\code{dates = NULL} is an alias for \code{field_period = NULL}, and either spelling
removes both the \code{field_period} and the legacy \code{dates} attribute from a data
frame.

A design object restored from a file written by surveycore 1.1.0 or earlier
cannot store dataset metadata. Reading such an object works, but a write
raises an error naming the remedy: rebuild the object with a constructor.
}
\examples{
# On a data frame, dataset metadata lives in whole-object attributes.
df <- data.frame(id = 1:5, y = c(2, 4, 3, 5, 1), w = rep(1, 5))
df <- set_dataset_metadata(
  df,
  survey_name = "Example Attitudes Survey 2026",
  vendor = "Ipsos KnowledgePanel"
)
extract_dataset_metadata(df)

# An ISO 8601 string is stored as a Date.
df <- set_dataset_metadata(df, field_start = "2026-02-10")
extract_dataset_metadata(df, field_start)

# Convention 2: one named list.
df <- set_dataset_metadata(df, list(field_end = "2026-03-04"))

# Convention 3: key plus value.
df <- set_dataset_metadata(
  df,
  key = c("data_name", "field_period"),
  value = list("Example (February-March 2026)", "February-March 2026")
)
extract_dataset_metadata(df, format = "data_frame")

# A NULL value deletes a key.
df <- set_dataset_metadata(df, vendor = NULL)
extract_dataset_metadata(df, vendor, fill = NA)

# The same API works on a survey design object.
d <- as_survey(df, weights = w)
d <- set_dataset_metadata(d, vendor = "Cint")
extract_dataset_metadata(d, vendor)
}
\seealso{
Other metadata:
\code{\link[=classify_question_type]{classify_question_type()}},
\code{\link[=extract_dataset_metadata]{extract_dataset_metadata()}},
\code{\link[=extract_higher_is]{extract_higher_is()}},
\code{\link[=extract_metadata]{extract_metadata()}},
\code{\link[=extract_missing_codes]{extract_missing_codes()}},
\code{\link[=extract_question_preface]{extract_question_preface()}},
\code{\link[=extract_reverse_coded]{extract_reverse_coded()}},
\code{\link[=extract_sata]{extract_sata()}},
\code{\link[=extract_universe]{extract_universe()}},
\code{\link[=extract_val_labels]{extract_val_labels()}},
\code{\link[=extract_var_label]{extract_var_label()}},
\code{\link[=extract_var_note]{extract_var_note()}},
\code{\link[=infer_question_prefaces]{infer_question_prefaces()}},
\code{\link[=set_higher_is]{set_higher_is()}},
\code{\link[=set_missing_codes]{set_missing_codes()}},
\code{\link[=set_question_preface]{set_question_preface()}},
\code{\link[=set_reverse_coded]{set_reverse_coded()}},
\code{\link[=set_sata]{set_sata()}},
\code{\link[=set_universe]{set_universe()}},
\code{\link[=set_val_labels]{set_val_labels()}},
\code{\link[=set_var_label]{set_var_label()}},
\code{\link[=set_var_note]{set_var_note()}},
\code{\link[=survey_metadata]{survey_metadata()}},
\code{\link[=survey_weighting_history]{survey_weighting_history()}}
}
\concept{metadata}
