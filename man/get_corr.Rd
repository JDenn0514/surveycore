% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-corr.R
\name{get_corr}
\alias{get_corr}
\title{Survey-Weighted Pearson Correlation}
\usage{
get_corr(
  design,
  x,
  group = NULL,
  format = c("long", "wide"),
  redundant = FALSE,
  diagonal = FALSE,
  variance = "ci",
  conf_level = 0.95,
  n_weighted = FALSE,
  decimals = NULL,
  min_cell_n = 30L,
  na.rm = TRUE,
  label_values = TRUE,
  label_vars = TRUE,
  name_style = "surveycore",
  ...,
  .id = ".survey",
  .on_missing = "error"
)
}
\arguments{
\item{design}{A survey design object: \code{survey_taylor}, \code{survey_replicate},
\code{survey_twophase}, or \code{survey_nonprob}.}

\item{x}{<\code{\link[tidyselect:language]{tidy-select}}> Two or more unquoted
numeric variable names. Non-numeric variables are dropped with a warning.
At least two numeric variables must remain.}

\item{group}{<\code{\link[tidyselect:language]{tidy-select}}> Optional grouping
variable(s). Combined with any grouping set by \code{group_by()}. Default
\code{NULL}.}

\item{format}{\code{"long"} (default) or \code{"wide"}. Long format returns one row
per variable pair with inference statistics. Wide format returns the
correlation matrix (\code{r} values only — no variance or inference columns).
When \code{group} is active, group columns are prepended in both formats.
Case-sensitive.}

\item{redundant}{Logical. If \code{FALSE} (default), each pair appears once
(lower triangle: pairs where var1 precedes var2 in input order). If
\code{TRUE}, both \verb{(A, B)} and \verb{(B, A)} are included (full directed pairs).
Only affects long format; wide format always shows the full symmetric
matrix.}

\item{diagonal}{Logical. If \code{FALSE} (default), self-correlations are
excluded (diagonal is \code{NA} in wide format). If \code{TRUE}, self-correlations
(r equals 1) are included.}

\item{variance}{\code{NULL} or a character vector of one or more of \code{"se"},
\code{"ci"}, \code{"var"}, \code{"cv"}, \code{"moe"}, \code{"deff"}. Default \code{"ci"}. CI bounds
use the Fisher Z transform (guaranteeing bounds in (-1, 1)). Only
applies to long format.}

\item{conf_level}{Numeric scalar in (0, 1). Default \code{0.95}.}

\item{n_weighted}{Logical. If \code{TRUE}, add an \code{n_weighted} column with the
pairwise sum of weights (both variables non-NA). Default \code{FALSE}.}

\item{decimals}{Integer or \code{NULL}. If an integer, rounds all numeric output
columns (e.g., \code{r}, \code{se}, \code{ci_low}, \code{ci_high}) to this many decimal
places. Default \code{NULL} (no rounding).}

\item{min_cell_n}{Integer. Minimum pairwise unweighted count before
\code{surveycore_warning_small_cell} fires. Default \code{30L} (AAPOR guidance).}

\item{na.rm}{Logical. If \code{TRUE} (default), pairs use complete cases for
each variable pair separately (pairwise deletion), and observations where
any group variable is \code{NA} are excluded from the output. If \code{FALSE},
pairwise complete cases are still used for each variable pair, and
observations where a group variable is \code{NA} are collected into their own
group row in the output (appearing after all non-\code{NA} group rows).}

\item{label_values}{Logical. If \code{TRUE} (default) and the grouping variable
has value labels, the group column is converted to a labelled factor.
Has no visible effect when no groups are active.}

\item{label_vars}{Logical. If \code{TRUE} (default) and variable labels are
set in metadata, \code{var1}/\code{var2} columns (long) and \code{variable} column
(wide) show labels instead of raw names. Falls back to raw names if
labels are unset.}

\item{name_style}{\code{"surveycore"} (default) or \code{"broom"}. When \code{"broom"},
renames \code{r} → \code{estimate}, \code{se} → \code{std.error}, etc. Only affects long
format.}

\item{...}{Unused. Reserved so that \code{.id} and \code{.on_missing} remain
named-only when a \code{survey_collection} is passed as \code{design}.}

\item{.id}{Character(1). Column name used to identify each survey when
\code{design} is a \code{\link{survey_collection}}. Default \code{".survey"}. Ignored when
\code{design} is a single survey.}

\item{.on_missing}{\code{"error"} (default) or \code{"skip"}. How to handle surveys
in a collection that lack one of the requested NSE variables. Ignored
when \code{design} is a single survey.}
}
\value{
A \code{survey_corr} tibble (also inheriting \code{survey_result}).

When \code{group} is active, group variable columns are prepended before all
other columns in both long and wide formats.

\strong{Long format} columns:
\itemize{
\item \verb{[group_cols...]} — group variable columns (when active), first.
\item \code{var1}, \code{var2} — variable names (or labels when
\code{label_vars = TRUE}).
\item \code{r} — Pearson correlation coefficient.
\item Variance columns (\code{se}, \code{var}, \code{cv}, \code{ci_low}, \code{ci_high}, \code{moe},
\code{deff}) — only those requested via \code{variance}.
\item \code{p_value} — two-tailed p-value.
\item \code{statistic} — t-statistic.
\item \code{df} — degrees of freedom for the t-test (n minus 2).
\item \code{n} — pairwise unweighted count.
\item \code{n_weighted} — pairwise sum of weights (only when requested).
}

\strong{Wide format} columns:
\itemize{
\item \verb{[group_cols...]} — group variable columns (when active), first.
\item \code{variable} — row variable names (or labels).
\item One column per focal variable, containing \code{r} values.
}

Use \code{meta(result)} to access design type, variable labels, and
\code{method} (\code{"pearson"}).
}
\description{
Compute pairwise Pearson correlations between two or more numeric variables
in a survey design, with design-based standard errors and confidence
intervals. Returns results in long or wide format.
}
\examples{
d <- as_survey(nhanes_2017, ids = sdmvpsu, weights = wtint2yr,
               strata = sdmvstra, nest = TRUE)
get_corr(d, x = c(ridageyr, bpxsy1))

# Wide correlation matrix
get_corr(d, x = c(ridageyr, bpxsy1), format = "wide")

# AAPOR-compliant
get_corr(d, x = c(ridageyr, bpxsy1),
         variance = c("ci", "moe"), n_weighted = TRUE)

}
\seealso{
Other analysis: 
\code{\link{clean}()},
\code{\link{get_anova}()},
\code{\link{get_diffs}()},
\code{\link{get_freqs}()},
\code{\link{get_means}()},
\code{\link{get_pairwise}()},
\code{\link{get_quantiles}()},
\code{\link{get_ratios}()},
\code{\link{get_t_test}()},
\code{\link{get_totals}()},
\code{\link{get_variance}()},
\code{\link{meta}()}
}
\concept{analysis}
