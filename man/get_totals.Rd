% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06-variance-estimation.R
\name{get_totals}
\alias{get_totals}
\title{Estimate Weighted Total for a Survey Design}
\usage{
get_totals(design, var, na.rm = TRUE)
}
\arguments{
\item{design}{A \code{survey_taylor} design object created by \code{\link[=as_survey]{as_survey()}}.}

\item{var}{<\code{\link[tidyselect:language]{tidy-select}}> A single unquoted
variable name to estimate the total of.}

\item{na.rm}{Logical. If \code{TRUE} (default), missing values are excluded
before computing the total. Set to \code{FALSE} to propagate \code{NA}.}
}
\value{
A named list with elements:
\describe{
\item{\code{variable}}{Character. Name of the estimated variable.}
\item{\code{total}}{Numeric. Weighted total estimate.}
\item{\code{se}}{Numeric. Standard error of the total.}
}
}
\description{
Computes the weighted total and its standard error for a single variable
using the appropriate variance estimator for the survey design type.
For \code{survey_taylor} designs, Taylor series linearization is used.
}
\examples{
df <- data.frame(
  psu = rep(1:5, each = 4),
  st  = rep(c("A", "B"), each = 10),
  wt  = rep(c(2, 3, 2, 4, 3), each = 4),
  y   = rnorm(20)
)
d <- as_survey(df, ids = psu, strata = st, weights = wt)
get_totals(d, y)

}
\seealso{
Other estimation: 
\code{\link{get_means}()}
}
\concept{estimation}
