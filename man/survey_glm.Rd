% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glm.R
\name{survey_glm}
\alias{survey_glm}
\title{Fit a Survey-Weighted Generalised Linear Model}
\usage{
survey_glm(
  design,
  formula = NULL,
  response = NULL,
  predictors = NULL,
  family = stats::gaussian(),
  na.action = stats::na.omit,
  start = NULL,
  etastart = NULL,
  mustart = NULL,
  control = list()
)
}
\arguments{
\item{design}{A survey design object created by \code{\link[=as_survey]{as_survey()}},
\code{\link[=as_survey_replicate]{as_survey_replicate()}}, \code{\link[=as_survey_twophase]{as_survey_twophase()}}, or
\code{\link[=as_survey_nonprob]{as_survey_nonprob()}}.}

\item{formula}{A model formula in standard R notation
(e.g. \code{y ~ x1 + x2}). Mutually exclusive with \code{response}/\code{predictors}.
If \code{NULL} and \code{response} is also \code{NULL}, errors with
\code{surveycore_error_formula_missing}.}

\item{response}{Character string naming the outcome variable.
Programmatic alternative to \code{formula}. Mutually exclusive with
\code{formula}. Use with \code{predictors} to build a model formula via
\code{reformulate(predictors, response)}. Suitable for \code{lapply()} iteration.}

\item{predictors}{Character vector of predictor variable names. Used with
\code{response} to build the model formula. If \code{response} is supplied and
\code{predictors} is \code{NULL}, an intercept-only model is fitted.}

\item{family}{A GLM family object specifying the error distribution and
link function. Default \code{gaussian()}. Any family accepted by
\code{\link[stats:glm]{stats::glm()}} is supported. For \code{binomial()} and \code{quasibinomial()}
families, the "non-integer #successes" warning is suppressed because
survey weights are non-integer by design.}

\item{na.action}{How to handle \code{NA} values in the model frame.
Default \code{na.omit} (silently drops rows with any \code{NA} in model
variables). \code{na.fail} errors with \code{surveycore_error_na_in_data} listing
the offending columns and NA counts. Note: \code{na.action} applies only to
model frame variables; survey weights are validated separately.}

\item{start}{Starting values for the coefficient vector.}

\item{etastart}{Starting values for the linear predictor.}

\item{mustart}{Starting values for the mean.}

\item{control}{A list of GLM control parameters passed to
\code{\link[stats:glm.control]{stats::glm.control()}}.}
}
\value{
A \link{survey_glm_fit} S7 object.
}
\description{
Fits a GLM to survey data, producing design-based coefficient estimates
and variance-covariance matrix via the Binder (1983) sandwich estimator.
All five surveycore design classes are supported.
}
\details{
\strong{Variance estimation:} Uses the Binder (1983) sandwich estimator, which
decomposes into per-observation score vectors passed to the Phase 0
variance machinery. The bread \verb{(X'W̃X)⁻¹} accounts for IRLS working
weights and is correct for all GLM families including binomial and
Poisson.

\strong{\code{binomial()} family:} Wraps the \code{stats::glm()} call in
\code{suppressWarnings()} to suppress the "non-integer #successes" warning
that fires for every survey-weighted binomial model.

\strong{Domain estimation:} Use \code{surveytidy::filter()} before calling
\code{survey_glm()}. The GLM is fit on in-domain rows only; variance
estimation uses the full design for correct design-based SEs.

\strong{Multinomial response:} \code{cbind()} on the LHS of \code{formula} is not
supported. Multinomial logistic regression is deferred to a later phase.
}
\examples{
d <- as_survey(gss_2024, ids = vpsu, weights = wtssps, strata = vstrat,
               nest = TRUE)

# Linear model: respondent age predicted by education and sex
fit <- survey_glm(d, age ~ educ + sex)
fit@coefficients
fit@vcov

# Programmatic interface — suitable for lapply()
results <- lapply(c("age", "educ"), function(v) {
  survey_glm(d, response = v, predictors = "sex")
})

}
\seealso{
Other constructors: 
\code{\link{as_survey}()},
\code{\link{as_survey_nonprob}()},
\code{\link{as_survey_replicate}()},
\code{\link{as_survey_twophase}()},
\code{\link{survey_data}()},
\code{\link{survey_glm_fit}()},
\code{\link{survey_nonprob}()},
\code{\link{survey_replicate}()},
\code{\link{survey_taylor}()},
\code{\link{survey_twophase}()}
}
\concept{constructors}
