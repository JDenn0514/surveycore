% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-methods-coef-vcov.R
\name{coef.survey_result}
\alias{coef.survey_result}
\title{Extract coefficients from a survey result object}
\usage{
\method{coef}{survey_result}(object, ...)
}
\arguments{
\item{object}{A \code{survey_result} object from any supported \verb{get_*()} function.}

\item{...}{Unused. Reserved for future extensions.}
}
\value{
A named numeric vector. Names follow variable-major, group-secondary
ordering matching \code{survey::coef.svyby()}. For grouped results, names are
\code{"group_value:variable_name"} (e.g., \code{"A:y1"}, \code{"B:y1"}). For ungrouped
results, names are the variable name (e.g., \code{"y1"}). See the spec for
class-specific naming rules.
}
\description{
Returns a named numeric vector of point estimates from a \code{survey_result}
object (produced by \code{get_means()}, \code{get_totals()}, \code{get_freqs()},
\code{get_ratios()}, \code{get_quantiles()}, \code{get_corr()}, \code{get_covariance()},
or \code{get_diffs()}).
}
\details{
Not all \code{survey_result} subclasses support \code{coef()}. \code{survey_t_test} and
\code{survey_pairwise} objects throw \code{surveycore_error_result_method_unsupported}.
Wide-format \code{survey_corr} objects (from \code{get_corr(format = "wide")}) also
throw this error. For those objects, access columns directly (e.g.,
\code{result$estimate}, \code{result$se}).
}
\examples{
d <- as_survey(
  nhanes_2017,
  ids = sdmvpsu,
  weights = wtint2yr,
  strata = sdmvstra,
  nest = TRUE
)
result <- get_means(d, ridageyr, variance = "se")
coef(result)
}
