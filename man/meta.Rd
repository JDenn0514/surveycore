% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09-meta.R
\name{meta}
\alias{meta}
\alias{meta.survey_result}
\title{Extract Metadata from a Survey Result}
\usage{
meta(x, ...)

\method{meta}{survey_result}(x, ...)
}
\arguments{
\item{x}{A \code{survey_result} object returned by any \verb{get_*()} function.}

\item{...}{Currently unused. Reserved for future extensions.}
}
\value{
A named list. Common fields present on every result:
\describe{
\item{\code{design_type}}{Character(1). Design class: \code{"taylor"},
\code{"replicate"}, \code{"twophase"}, \code{"srs"}, or \code{"calibrated"}.}
\item{\code{conf_level}}{Numeric(1). Confidence level used (e.g. \code{0.95}).}
\item{\code{call}}{Language. Matched call to the \verb{get_*()} function.}
\item{\code{group_names}}{Character vector. Grouping variable names;
\code{character(0)} if none.}
\item{\code{group_labels}}{Named list. Group variable → label; \code{NULL} values
for unlabeled group variables.}
\item{\code{n_respondents}}{Integer(1). Total rows in the design, regardless
of groups, domain status, or weights.}
\item{\code{value_labels}}{Named list. One key per focal variable; value is a
named vector of label → raw value mappings, or \code{NULL} for
numeric/unlabelled variables.}
}
Additional function-specific fields are described in the Phase 1
specification.
}
\description{
Retrieves the structured metadata list attached to a survey result object
returned by any \verb{get_*()} analysis function.
}
\details{
This is the \strong{only} supported way to access result metadata — do not use
\code{attr(result, ".meta")} directly.
}
\examples{
# Construct a minimal survey_result to illustrate meta():
result <- structure(
  tibble::tibble(mean = 42.0, se = 1.5, n = 100L),
  .meta = list(
    design_type   = "taylor",
    conf_level    = 0.95,
    call          = quote(get_means(d, x)),
    group_names   = character(0),
    group_labels  = list(),
    n_respondents = 100L,
    variable      = "x",
    variable_label   = NULL,
    question_preface = NULL,
    value_labels     = list(x = NULL)
  ),
  class = c("survey_means", "survey_result", "tbl_df", "tbl", "data.frame")
)
meta(result)$design_type    # "taylor"
meta(result)$n_respondents  # 100L
meta(result)$conf_level     # 0.95

}
\seealso{
Other analysis: 
\code{\link{get_corr}()},
\code{\link{get_freqs}()},
\code{\link{get_means}()},
\code{\link{get_quantiles}()},
\code{\link{get_ratios}()},
\code{\link{get_totals}()}
}
\concept{analysis}
