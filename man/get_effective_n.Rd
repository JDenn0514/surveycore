% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-effective-n.R
\name{get_effective_n}
\alias{get_effective_n}
\title{Effective Sample Size for a Survey Design}
\usage{
get_effective_n(
  design,
  x = NULL,
  group = NULL,
  method = c("kish", "deff"),
  na.rm = TRUE,
  decimals = NULL,
  min_cell_n = 30L,
  ...,
  .id = NULL,
  .if_missing_var = NULL
)
}
\arguments{
\item{design}{A survey design object: \code{survey_taylor}, \code{survey_replicate},
\code{survey_twophase}, \code{survey_nonprob}, or a \code{survey_collection}.}

\item{x}{<\code{\link[tidyselect:language]{tidy-select}}> A single unquoted numeric
variable name. Required when \code{method = "deff"}; ignored (with a message)
when \code{method = "kish"}. Default \code{NULL}.}

\item{group}{<\code{\link[tidyselect:language]{tidy-select}}> Optional grouping
variable(s). Combined with any grouping set by \code{group_by()}. Default
\code{NULL}.}

\item{method}{Character(1). \code{"kish"} (default) or \code{"deff"}. Controls the
effective-N formula. Matched via \code{match.arg()}.}

\item{na.rm}{Logical. If \code{TRUE} (default), exclude observations with \code{NA}
weights or group variables from the Kish computation; passed to
\code{get_means()} for the DEFF computation.}

\item{decimals}{Integer or \code{NULL}. Rounds \code{n_eff} and deff columns to this
many decimal places. \code{n} is always integer and is never rounded. Default
\code{NULL}.}

\item{min_cell_n}{Integer. Minimum unweighted cell count before
\code{surveycore_warning_small_cell} fires (Kish method only). Default \code{30L}.}

\item{...}{Unused. Reserved so that \code{.id} and \code{.if_missing_var} remain
named-only when a \code{survey_collection} is passed.}

\item{.id}{Character(1) or \code{NULL}. Column name identifying each survey in a
\code{survey_collection}. Default \code{NULL} (uses the collection's stored \verb{@id}).}

\item{.if_missing_var}{\code{"error"}, \code{"skip"}, or \code{NULL}. Handling for
surveys in a collection that lack \code{x}. Default \code{NULL}.}
}
\value{
A \code{survey_effective_n} tibble (also inheriting \code{survey_result}).
Columns, in order:
\itemize{
\item \verb{[.id]} — survey identifier column (when \code{design} is a collection).
\item \verb{[group_cols...]} — group variable columns (when grouping is active).
\item \code{n} — integer. Unweighted count of observations.
\item \code{n_eff} — numeric. Effective sample size.
\item \code{deff_kish} — numeric. Weight-based design effect (\code{n / n_eff}).
Present when \code{method = "kish"} only.
\item \code{deff} — numeric. Full design effect (\code{Var_design / Var_SRS}).
Present when \code{method = "deff"} only.
}
Use \code{meta(result)$method} to retrieve the formula used. For DEFF,
\code{meta(result)$x} is a named list with variable metadata.
}
\description{
Computes the effective sample size of a survey design using either the
Kish (1965) weight-only approximation (\code{method = "kish"}) or the full
design-effect-based formula for a specified variable (\code{method = "deff"}).
}
\details{
The \strong{Kish method} (\code{method = "kish"}) computes effective N from survey
weights alone: \code{n_eff = sum(w)^2 / sum(w^2)}. It captures only weight variation.
For clustered designs with equal weights, \code{deff_kish = 1.0} even when the
true design effect is substantially greater due to clustering. Use
\code{method = "deff"} to capture the full design effect for a specific
analysis variable.

The \strong{DEFF method} (\code{method = "deff"}) computes effective N as
\code{n_eff = n / DEFF}, where \code{DEFF = Var_design / Var_SRS} for variable \code{x}.
It captures clustering, stratification, and weight variation jointly.
}
\examples{
d <- as_survey(
  nhanes_2017,
  ids = sdmvpsu,
  weights = wtint2yr,
  strata = sdmvstra,
  nest = TRUE
)

# Kish effective N (weight-only approximation)
get_effective_n(d)

# Kish effective N by group (one row per level of riagendr)
get_effective_n(d, group = riagendr)

# Full DEFF effective N for a specific variable
get_effective_n(d, ridageyr, method = "deff")
}
\seealso{
Other analysis: 
\code{\link{SE}()},
\code{\link{clean}()},
\code{\link{get_anova}()},
\code{\link{get_corr}()},
\code{\link{get_covariance}()},
\code{\link{get_diffs}()},
\code{\link{get_freqs}()},
\code{\link{get_means}()},
\code{\link{get_pairwise}()},
\code{\link{get_quantiles}()},
\code{\link{get_ratios}()},
\code{\link{get_t_test}()},
\code{\link{get_totals}()},
\code{\link{get_variance}()},
\code{\link{meta}()}
}
\concept{analysis}
