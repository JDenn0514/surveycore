% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03-constructors.R
\name{as_survey_srs}
\alias{as_survey_srs}
\title{Create a Simple Random Sample Survey Design}
\usage{
as_survey_srs(data, weights = NULL, probs = NULL, fpc = NULL)
}
\arguments{
\item{data}{A \code{data.frame} containing the survey responses. Must have at
least one row and unique column names.}

\item{weights}{<\code{\link[tidyselect:language]{tidy-select}}> Sampling weight
column (a single column, values strictly > 0). Supply either \code{weights} or
\code{probs}, not both. If both are \code{NULL}, uniform weights (\verb{= 1}) are
auto-assigned and a warning is issued; population totals will then
equal sample totals, not estimated population totals.}

\item{probs}{<\code{\link[tidyselect:language]{tidy-select}}> Sampling probability
column (a single column, values in (0, 1]). Converted to weights
\verb{= 1/probs} and stored internally as \code{..surveycore_wt..}. Supply either
\code{weights} or \code{probs}, not both.}

\item{fpc}{<\code{\link[tidyselect:language]{tidy-select}}> Finite population
correction column (a single column). Accepts either total population size
(all values > 1) or sampling fraction (all values in (0, 1]). Cannot mix
the two types. Cannot contain \code{NA} or non-positive values. If any FPC
value (of population-size type) is less than the sample size, an error is
thrown.}
}
\value{
A \code{survey_srs} object.
}
\description{
Creates a survey design object for equal-probability simple random samples
(SRS). Computes variances using the classical SRS formula
\verb{var(ȳ) = (1 - f) × s² / n}, where \verb{s²} is the unweighted sample variance,
\code{n} is the sample size, and \code{f = n/N} is the sampling fraction (0 when
population size is unknown).
}
\examples{
# Minimal: no weights, no FPC (uniform weights auto-assigned)
d <- suppressWarnings(as_survey_srs(data.frame(y = 1:5)))

}
\seealso{
\code{\link[=as_survey]{as_survey()}} for designs with cluster or stratification structure,
\code{\link[=as_survey_rep]{as_survey_rep()}} for replicate-weight designs

Other constructors: 
\code{\link{as_survey}()},
\code{\link{as_survey_calibrated}()},
\code{\link{as_survey_rep}()},
\code{\link{as_survey_twophase}()},
\code{\link{survey_calibrated}()},
\code{\link{survey_data}()},
\code{\link{survey_replicate}()},
\code{\link{survey_srs}()},
\code{\link{survey_taylor}()},
\code{\link{survey_twophase}()}
}
\concept{constructors}
