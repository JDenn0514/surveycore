% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-methods-coef-vcov.R
\name{confint.survey_result}
\alias{confint.survey_result}
\title{Confidence intervals for survey result objects}
\usage{
\method{confint}{survey_result}(object, parm, level = 0.95, ...)
}
\arguments{
\item{object}{A \code{survey_result} object from any supported \verb{get_*()} function.}

\item{parm}{A numeric, integer, or character vector selecting a subset of
parameters, or missing (all parameters). Character \code{parm} is matched
against \code{names(coef(object))}.}

\item{level}{Confidence level, strictly between 0 and 1. Default \code{0.95}.}

\item{...}{Unused.}
}
\value{
A numeric matrix with two columns. Column names are
\code{"2.5 \%"} and \code{"97.5 \%"} for \code{level = 0.95} (matching
\code{stats::confint.default()} format). Rownames are
\code{names(coef(object))[parm]}. Dimensions: \verb{length(parm) × 2}.
}
\description{
Computes confidence intervals for the parameters of a \code{survey_result} object
using the t-distribution with per-parameter degrees of freedom stored in
\code{attr(object, ".survey_result")$df}.
}
\details{
For non-calibrated Taylor, replicate, and nonprob designs, \code{df = Inf} is
stored, so \code{qt(1 - (1 - level)/2, df = Inf)} equals \code{qnorm(...)} — a normal
approximation. For calibrated Taylor designs, per-cell finite df values are
used.

When \code{SE(object)[i]} is \code{NA}, the corresponding CI row is \code{c(NA, NA)}.
}
\examples{
d <- as_survey(
  nhanes_2017,
  ids = sdmvpsu,
  weights = wtint2yr,
  strata = sdmvstra,
  nest = TRUE
)
result <- get_means(d, ridageyr, variance = "se")
confint(result)
confint(result, level = 0.90)
}
