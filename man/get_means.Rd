% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-means.R
\name{get_means}
\alias{get_means}
\title{Weighted Mean for a Survey Design}
\usage{
get_means(
  design,
  x,
  group = NULL,
  variance = "ci",
  conf_level = 0.95,
  n_weighted = FALSE,
  decimals = NULL,
  min_cell_n = 30L,
  na.rm = TRUE,
  label_values = TRUE,
  label_vars = TRUE,
  name_style = "surveycore",
  ...,
  .id = ".survey",
  .on_missing = "error"
)
}
\arguments{
\item{design}{A survey design object: \code{survey_taylor}, \code{survey_replicate},
\code{survey_twophase}, or \code{survey_nonprob}.}

\item{x}{<\code{\link[tidyselect:language]{tidy-select}}> A single unquoted numeric
variable name. Must resolve to exactly one numeric column.}

\item{group}{<\code{\link[tidyselect:language]{tidy-select}}> Optional grouping
variable(s). Combined with any grouping set by \code{group_by()}. Default
\code{NULL}.}

\item{variance}{\code{NULL} or a character vector of one or more of \code{"se"},
\code{"ci"}, \code{"var"}, \code{"cv"}, \code{"moe"}, \code{"deff"}. Controls which uncertainty
columns appear in the output. Default \code{"ci"}.}

\item{conf_level}{Numeric scalar in (0, 1). Confidence level for intervals.
Default \code{0.95}.}

\item{n_weighted}{Logical. If \code{TRUE}, add an \code{n_weighted} column with the
sum of weights for non-NA observations in each group. Default \code{FALSE}.}

\item{decimals}{Integer or \code{NULL}. If an integer, rounds all numeric output
columns (e.g., \code{mean}, \code{se}, \code{ci_low}, \code{ci_high}) to this many decimal
places. Default \code{NULL} (no rounding).}

\item{min_cell_n}{Integer. Minimum unweighted cell count before
\code{surveycore_warning_small_cell} fires. Default \code{30L} (AAPOR guidance).}

\item{na.rm}{Logical. If \code{TRUE} (default), \code{NA} values are excluded from
analysis: observations where the analysis variable is \code{NA} are dropped
from calculations, and observations where any group variable is \code{NA} are
excluded from the output. If \code{FALSE}, \code{NA} observations in the analysis
variable are included in calculations, and observations where a group
variable is \code{NA} are collected into their own group row in the output
(appearing after all non-\code{NA} group rows).}

\item{label_values}{Logical. Accepted for API uniformity; has no visible
effect since \code{get_means()} output contains no categorical value cells.
Default \code{TRUE}.}

\item{label_vars}{Logical. Accepted for API uniformity; has no visible
effect since \code{get_means()} output contains no variable-name value cells.
Default \code{TRUE}.}

\item{name_style}{\code{"surveycore"} (default) or \code{"broom"}. When \code{"broom"},
renames \code{mean} → \code{estimate}, \code{se} → \code{std.error}, etc.}

\item{...}{Unused. Reserved so that \code{.id} and \code{.on_missing} remain
named-only when a \code{survey_collection} is passed as \code{design}.}

\item{.id}{Character(1). Column name used to identify each survey when
\code{design} is a \code{\link{survey_collection}}. Default \code{".survey"}. Ignored when
\code{design} is a single survey.}

\item{.on_missing}{\code{"error"} (default) or \code{"skip"}. How to handle surveys
in a collection that lack one of the requested NSE variables. Ignored
when \code{design} is a single survey.}
}
\value{
A \code{survey_means} tibble (also inheriting \code{survey_result}). Columns:
\itemize{
\item \verb{[group_cols...]} — group variable columns (when active), first.
\item \code{mean} — weighted mean estimate.
\item Variance columns (\code{se}, \code{var}, \code{cv}, \code{ci_low}, \code{ci_high}, \code{moe},
\code{deff}) — only those requested via \code{variance}.
\item \code{n} — unweighted count of non-NA observations used in the estimate.
\item \code{n_weighted} — sum of weights (only when requested).
}
The variable name is stored in \code{meta(result)$variable}, not as a column.
Use \code{meta(result)} to access design type, variable labels, and other
metadata.
}
\description{
Compute the weighted mean of a single numeric variable in a survey design,
with optional grouping, uncertainty quantification, and metadata-driven
labelling.
}
\examples{
d <- as_survey(nhanes_2017, ids = sdmvpsu, weights = wtint2yr,
               strata = sdmvstra, nest = TRUE)
get_means(d, ridageyr)

# With grouped estimate
get_means(d, ridageyr, group = riagendr)

# AAPOR-compliant
get_means(d, ridageyr, variance = c("ci", "moe"), n_weighted = TRUE)

}
\seealso{
Other analysis: 
\code{\link{clean}()},
\code{\link{get_anova}()},
\code{\link{get_corr}()},
\code{\link{get_diffs}()},
\code{\link{get_freqs}()},
\code{\link{get_pairwise}()},
\code{\link{get_quantiles}()},
\code{\link{get_ratios}()},
\code{\link{get_t_test}()},
\code{\link{get_totals}()},
\code{\link{meta}()}
}
\concept{analysis}
