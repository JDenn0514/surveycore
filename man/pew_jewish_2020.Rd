% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{pew_jewish_2020}
\alias{pew_jewish_2020}
\title{Pew Jewish Americans 2020}
\format{
A data frame with 5,881 rows and 130 variables.

\strong{Design variables (101 columns):}
\describe{
\item{extweight}{Full-sample base weight. Use for all estimates.}
}
\code{extweight1}–\code{extweight100}: 100 jackknife replicate weights for variance
estimation. Use with \code{as_survey_rep(..., type = "JK1")}.

\strong{Identity and classification:}
\describe{
\item{qkey}{Unique respondent identifier.}
\item{jewishcat}{Jewish identity category: \code{1} = Jews by religion,
\code{2} = Jews of no religion, \code{3} = Jewish background,
\code{4} = Jewish affinity, \code{5} = Not Jewish.}
\item{finalmode}{Collection mode: \code{1} = CAWI (online),
\code{2} = Teleform (mail),
\code{3} = Screener CAWI + Extended Teleform.}
}

\strong{Geography and demographics:}
\describe{
\item{region}{Census region: \code{1} = Northeast, \code{2} = Midwest,
\code{3} = South, \code{4} = West.}
\item{sexask}{Sex: \code{1} = male, \code{2} = female, \code{99} = not answered.}
\item{age4cat}{Age: \code{1} = 18-29, \code{2} = 30-49, \code{3} = 50-64, \code{4} = 65+;
\code{999} = no answer.}
\item{educ4cat}{Education: \code{1} = HS or less, \code{2} = some college,
\code{3} = college graduate, \code{4} = postgraduate; \code{99} = no answer.}
\item{religmod}{Current religion (24 categories including Jewish
subgroups and combinations).}
\item{hisp}{Hispanic origin: \code{1} = yes, \code{2} = no.}
\item{racecmb}{Race (5 categories).}
\item{racethn}{Race-ethnicity (4 categories).}
}

\strong{Attitudes:}
\describe{
\item{presapp}{Presidential approval (Trump): \code{1} = Strongly approve,
\code{2} = Somewhat approve, \code{3} = Somewhat disapprove,
\code{4} = Strongly disapprove.}
\item{track}{Right track/wrong track: \code{1} = Right direction,
\code{2} = Wrong track.}
\item{satisfpersmod}{Personal life satisfaction: \code{1} = Excellent,
\code{2} = Good, \code{3} = Only fair, \code{4} = Poor.}
\item{localrating}{Community as a place to live: \code{1} = Excellent,
\code{2} = Good, \code{3} = Only fair, \code{4} = Poor.}
}

\strong{Battery 1 — Religious identity (select-all-that-apply, 4 variables):}
\code{relconsider_a}–\code{relconsider_d} share \code{question_preface} = \code{"Aside from religion, do you consider yourself to be any of the following in any way (for example, ethnically, culturally, or because of your family's background)?"} Values: \code{1} = Selected, \code{2} = Not selected, \code{99} = Refused.
\describe{
\item{relconsider_a}{Jewish.}
\item{relconsider_b}{Catholic.}
\item{relconsider_c}{Mormon.}
\item{relconsider_d}{Muslim.}
}

\strong{Battery 2 — Religious background (select-all-that-apply, 4 variables):}
\code{relraised_a}–\code{relraised_d} share \code{question_preface} = \code{"Please indicate whether you were raised in any of the following traditions or had a parent from any of the following backgrounds."} Values: \code{1} = Selected,
\code{2} = Not selected, \code{99} = Refused.
\describe{
\item{relraised_a}{Jewish.}
\item{relraised_b}{Catholic.}
\item{relraised_c}{Mormon.}
\item{relraised_d}{Muslim.}
}

\strong{Battery 3 — Discrimination perceptions (rating scale, 6 variables):}
\code{discrim_a}–\code{discrim_f} share \code{question_preface} = \code{"Please tell us how much discrimination there is against each of these groups in our society today."} Values: \code{1} = A lot, \code{2} = Some, \code{3} = Not much,
\code{4} = None at all, \code{99} = Not answered.
\describe{
\item{discrim_a}{Evangelical Christians.}
\item{discrim_b}{Muslims.}
\item{discrim_c}{Jews.}
\item{discrim_d}{Blacks.}
\item{discrim_e}{Hispanics.}
\item{discrim_f}{Gays and lesbians.}
}
}
\source{
Pew Research Center. Jewish Americans in 2020 (Extended Dataset).
\url{https://www.pewresearch.org/datasets} (free account required to
download raw data; the processed \code{.rda} is included in the package).
Prepared by \verb{data-raw/prepare-pew-jewish-2020.R}.
}
\usage{
pew_jewish_2020
}
\description{
The extended survey dataset from Pew Research Center's 2019-2020 Survey
of U.S. Jews, fielded November 19, 2019 – June 3, 2020 (n = 5,881).
Respondents were drawn from a national, stratified random sample of
residential mailing addresses with oversampling of households likely to
contain Jewish respondents. The dataset carries 100 jackknife replicate
weights alongside the main weight.
}
\details{
\strong{Survey design:} Jackknife replication — use \code{as_survey_rep()} with all
100 replicate weights:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{svy <- as_survey_rep(
  pew_jewish_2020,
  weights    = extweight,
  repweights = starts_with("extweight") & !matches("^extweight$"),
  type       = "JK1"
)
}\if{html}{\out{</div>}}

\strong{Jewish identity classification:} The \code{jewishcat} variable classifies
respondents into five mutually exclusive categories used in the published
Pew report. Use \code{jewishcat} rather than constructing your own
classification from the raw religion variables.

\strong{Metadata:}
All columns carry variable labels and value labels as R attributes from the
original Stata file. The three battery variable groups additionally carry a
\code{"question_preface"} attribute with the shared question stem. All three
attribute types are automatically extracted into surveycore's metadata
system when you call \code{as_survey_rep()}.
\itemize{
\item \strong{Variable labels} (\code{"label"} attribute): A human-readable description of
each column — for battery items this is the unique item text (e.g.,
\code{"Jewish"}). Example: \code{attr(pew_jewish_2020$relconsider_a, "label")}
returns \code{"Jewish"}.
\item \strong{Value labels} (\code{"labels"} attribute): A named numeric vector mapping
each code to its meaning. Example:
\code{attr(pew_jewish_2020$relconsider_a, "labels")} returns
\code{c(Selected = 1, "Not selected" = 2, Refused = 99)}.
\item \strong{Question preface} (\code{"question_preface"} attribute): The shared question
stem for each battery group. Example:
\code{attr(pew_jewish_2020$discrim_a, "question_preface")} returns
\code{"Please tell us how much discrimination there is against each of these groups in our society today."}.
}
}
\examples{
# Design variables
head(pew_jewish_2020[, c("qkey", "extweight", "jewishcat")])

# Confirm 100 replicate weights are present
sum(grepl("^extweight[0-9]", names(pew_jewish_2020)))

# Inspect variable label (unique item text for battery variable)
attr(pew_jewish_2020$discrim_a, "label")

# Inspect value labels
attr(pew_jewish_2020$discrim_a, "labels")

# Inspect question preface (shared stem across the battery)
attr(pew_jewish_2020$discrim_a, "question_preface")

# Jewish identity distribution (use jewishcat, not raw religion vars)
table(pew_jewish_2020$jewishcat)
}
\keyword{datasets}
