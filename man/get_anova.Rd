% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glm-anova.R
\name{get_anova}
\alias{get_anova}
\title{Design-Based Analysis of Variance for Survey GLM Fits}
\usage{
get_anova(
  model,
  model2 = NULL,
  method = c("LRT", "Wald"),
  test = c("F", "Chisq"),
  null = NULL,
  tolerance = sqrt(.Machine$double.eps),
  decimals = NULL,
  label_vars = TRUE,
  name_style = "surveycore"
)
}
\arguments{
\item{model}{A \link{survey_glm_fit} object.}

\item{model2}{Optional second \link{survey_glm_fit}. When supplied, \code{get_anova()}
runs in nested-comparison mode (one result row); otherwise it runs in
sequential mode (one row per term).}

\item{method}{Character(1). \code{"LRT"} (default) or \code{"Wald"}.}

\item{test}{Character(1). \code{"F"} (default) or \code{"Chisq"} reference
distribution.}

\item{null}{Numeric or \code{NULL}. Hypothesized value for the tested
coefficients (Wald only). Ignored in LRT mode (errors if non-NULL).}

\item{tolerance}{Numeric(1). Reciprocal-condition-number threshold for the
naive-covariance near-singular gate in the Rao-Scott LRT. Default
\code{sqrt(.Machine$double.eps)}.}

\item{decimals}{Integer(1) or \code{NULL}. Round double output columns.}

\item{label_vars}{Logical(1). When \code{TRUE}, compose term-row labels from
\verb{@metadata@variable_labels} for the \code{term} column. Default \code{TRUE}.}

\item{name_style}{Character(1). \code{"surveycore"} (default) or \code{"broom"}.}
}
\value{
A \code{survey_anova} tibble with columns \code{term}, \code{statistic}, \code{df},
\code{ddf}, \code{deff}, \code{p_value}, \code{stars} and a \code{.meta} attribute.
}
\description{
Rao-Scott design-based ANOVA for \code{\link[=survey_glm]{survey_glm()}} fits. Supports the four
method x test combinations shared with \code{survey::anova.svyglm()}: Rao-Scott
working-LRT with F or Chisq reference, and design-based Wald with F or
Chisq reference. Sequential mode tests each term in the formula by right-
to-left drop; comparison mode tests the added terms between two nested
fits.
}
\examples{
# Filter to complete cases on modeled columns so the sequential refit
# operates on the same rows as the full model (required by the LRT).
gss_cc <- gss_2024[
  stats::complete.cases(gss_2024[, c("age", "sex", "educ")]),
]
gss_design <- as_survey(
  gss_cc, ids = vpsu, weights = wtssps,
  strata = vstrat, nest = TRUE
)
fit <- survey_glm(gss_design, age ~ sex + educ)
get_anova(fit)

}
\seealso{
Other analysis: 
\code{\link{clean}()},
\code{\link{get_corr}()},
\code{\link{get_diffs}()},
\code{\link{get_freqs}()},
\code{\link{get_means}()},
\code{\link{get_pairwise}()},
\code{\link{get_quantiles}()},
\code{\link{get_ratios}()},
\code{\link{get_t_test}()},
\code{\link{get_totals}()},
\code{\link{meta}()}
}
\concept{analysis}
