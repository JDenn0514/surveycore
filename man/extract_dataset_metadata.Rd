% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-metadata.R
\name{extract_dataset_metadata}
\alias{extract_dataset_metadata}
\title{Extract Dataset-Level Metadata}
\usage{
extract_dataset_metadata(x, ..., key = NULL, format = "list", fill = NULL)
}
\arguments{
\item{x}{A survey design object or \code{data.frame}.}

\item{...}{Keys to return, as bare names (\code{vendor}) or strings
(\code{"vendor"}). Supports \verb{!!!} splicing of a character vector of names.
Matching is by plain name and is \strong{not} tidy-select: a key is not a
column, so selection helpers such as \code{all_of()} are rejected. Repeated
requests are deduplicated, keeping the first position. If empty, returns
every key that is set.}

\item{key}{\code{character} vector of key names, or \code{NULL} (default). A
programmatic alternative to \code{...}; supplying both is an error. Must be
supplied by name, because it follows \code{...}.}

\item{format}{\code{character(1)}. Output shape: \code{"list"} (default) or
\code{"data_frame"}. Must be supplied by name, because it follows \code{...}.}

\item{fill}{\code{NULL} (default), \code{NA}, or \code{NA_character_}. Has two effects.
For keys named in \code{...} or \code{key} that are not set: \code{NULL} omits them,
while \code{NA} and \code{NA_character_} include them with a missing value. With an
empty request, a non-\code{NULL} \code{fill} returns the full six-key schema with
every absent key filled — the codebook idiom. Must be supplied by name,
because it follows \code{...}.}
}
\value{
\itemize{
\item \code{"list"} (default): named list of the stored values, in request order, or
canonical key order for an empty request. Date keys are \code{Date(1)}. A
filled absent key is type-matched: \code{as.Date(NA)} for \code{field_start} and
\code{field_end}, \code{NA_character_} for the four character keys. Empty result:
\code{list()}.
\item \code{"data_frame"}: tibble with columns \code{key} and \code{value}, both character. A
\code{Date} renders through \code{format()} as \code{YYYY-MM-DD}, and a filled absent key
is always \code{NA_character_} regardless of its type. Empty result: zero-row
tibble with both columns.
}
}
\description{
Returns whole-dataset metadata — the survey name, the display name, the
fielding vendor, and the field dates — from a survey design object or a
data frame.
}
\details{
The key vocabulary is closed. Exactly six keys are valid, and this is their
canonical order:

\describe{
\item{\code{survey_name}}{\code{character(1)}. Full formal survey name.}
\item{\code{data_name}}{\code{character(1)}. Display label for this dataset.}
\item{\code{vendor}}{\code{character(1)}. Fielding vendor.}
\item{\code{field_start}}{\code{Date(1)}. First day in the field.}
\item{\code{field_end}}{\code{Date(1)}. Last day in the field.}
\item{\code{field_period}}{\code{character(1)}. Prose field period, for display.}
}

\code{survey_name} and \code{data_name} are independent. This function never reads one
to compute the other, and it never composes a label from the field dates.

On a survey design object the stored metadata is the single source of truth;
attributes on the underlying data frame are not consulted. On a data frame
the six attribute names above are read directly, plus the pre-1.2.0 name
\code{dates}, which is read as \code{field_period} when no \code{field_period} attribute is
present. Any other attribute is ignored. An attribute whose value fails the
type rules above is dropped silently — this function never warns.

A design object restored from a file written by surveycore 1.1.0 or earlier
reports no dataset metadata rather than failing.

\code{extract_metadata()} is deliberately separate: it reports per-variable
fields only, and a dataset-level key is not a variable.
}
\examples{
# On a data frame, dataset metadata lives in whole-object attributes.
df <- data.frame(id = 1:5, y = c(2, 4, 3, 5, 1), w = rep(1, 5))
attr(df, "survey_name") <- "Example Attitudes Survey 2026"
attr(df, "vendor") <- "Ipsos KnowledgePanel"
attr(df, "field_start") <- as.Date("2026-02-10")
attr(df, "field_end") <- as.Date("2026-03-04")

extract_dataset_metadata(df)
extract_dataset_metadata(df, vendor)
extract_dataset_metadata(df, key = c("field_start", "field_end"))
extract_dataset_metadata(df, format = "data_frame")

# An unset key is omitted by default, or filled on request.
extract_dataset_metadata(df, data_name)
extract_dataset_metadata(df, data_name, fill = NA)

# A non-NULL fill plus an empty request gives the whole six-key schema.
extract_dataset_metadata(df, fill = NA, format = "data_frame")
}
\seealso{
Other metadata:
\code{\link[=classify_question_type]{classify_question_type()}},
\code{\link[=extract_higher_is]{extract_higher_is()}},
\code{\link[=extract_metadata]{extract_metadata()}},
\code{\link[=extract_missing_codes]{extract_missing_codes()}},
\code{\link[=extract_question_preface]{extract_question_preface()}},
\code{\link[=extract_reverse_coded]{extract_reverse_coded()}},
\code{\link[=extract_sata]{extract_sata()}},
\code{\link[=extract_universe]{extract_universe()}},
\code{\link[=extract_val_labels]{extract_val_labels()}},
\code{\link[=extract_var_label]{extract_var_label()}},
\code{\link[=extract_var_note]{extract_var_note()}},
\code{\link[=infer_question_prefaces]{infer_question_prefaces()}},
\code{\link[=set_higher_is]{set_higher_is()}},
\code{\link[=set_missing_codes]{set_missing_codes()}},
\code{\link[=set_question_preface]{set_question_preface()}},
\code{\link[=set_reverse_coded]{set_reverse_coded()}},
\code{\link[=set_sata]{set_sata()}},
\code{\link[=set_universe]{set_universe()}},
\code{\link[=set_val_labels]{set_val_labels()}},
\code{\link[=set_var_label]{set_var_label()}},
\code{\link[=set_var_note]{set_var_note()}},
\code{\link[=survey_metadata]{survey_metadata()}},
\code{\link[=survey_weighting_history]{survey_weighting_history()}}
}
\concept{metadata}
