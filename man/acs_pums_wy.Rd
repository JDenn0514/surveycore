% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{acs_pums_wy}
\alias{acs_pums_wy}
\title{ACS PUMS 2022 1-Year: Wyoming Persons}
\format{
A data frame with 5,962 rows and 96 variables. The first 3 variables
are design variables, the next 80 are replicate weights, and the final 13
are person characteristics:

\strong{Design variables:}
\describe{
\item{puma}{Public Use Microdata Area code. Use as the cluster ID (PSU)
for variance estimation.}
\item{st}{State FIPS code (all \code{56} = Wyoming).}
\item{pwgtp}{Person weight. Represents the number of people in the
Wyoming population that this record represents.}
}

\strong{Replicate weights (\code{pwgtp1}–\code{pwgtp80}):}
Eighty successive difference replicate weights for variance estimation via
the successive difference replication (SDR) method. Use with
\code{as_survey_rep(..., type = "successive-difference")}.

\strong{Person characteristics:}
\describe{
\item{agep}{Age (0–99 years).}
\item{sex}{Sex: \code{1} = male, \code{2} = female.}
\item{rac1p}{Recoded detailed race: \code{1} = White alone, \code{2} = Black or
African American alone, \code{3} = American Indian alone, \code{6} = Asian alone,
\code{9} = Two or more races.}
\item{hisp}{Recoded Hispanic origin: \code{01} = Not Spanish/Hispanic/Latino;
\code{02}–\code{24} = specific Hispanic origin.}
\item{schl}{Educational attainment (24 categories): \code{01} = no schooling,
\code{16} = regular high school diploma, \code{21} = bachelor's degree,
\code{24} = doctorate degree.}
\item{esr}{Employment status recode: \code{1} = civilian employed at work,
\code{2} = civilian employed with job but not at work, \code{3} = unemployed,
\code{4} = Armed Forces at work, \code{5} = Armed Forces not at work,
\code{6} = Not in labor force.}
\item{pincp}{Total person income in the past 12 months (dollars, signed;
negative values indicate a net loss). Multiply by \code{adjinc / 1e6} to
adjust to constant dollars.}
\item{wagp}{Wages or salary income in the past 12 months (dollars).
\code{NA} if not applicable.}
\item{hicov}{Health insurance coverage: \code{1} = with health insurance,
\code{2} = without health insurance.}
\item{dis}{Disability recode: \code{1} = with a disability, \code{2} = without a
disability.}
\item{povpip}{Income-to-poverty ratio (0–501; \code{501} means 501\% or more).}
\item{wkhp}{Usual hours worked per week in the past 12 months. \code{NA} if
not in the labor force.}
\item{adjinc}{Adjustment factor for income and earnings. Divide by
1,000,000 and multiply income variables to convert to 2022 constant
dollars.}
}
}
\source{
U.S. Census Bureau. 2022 ACS 1-Year PUMS.
\url{https://www.census.gov/programs-surveys/acs/microdata/access.html}
}
\usage{
acs_pums_wy
}
\description{
All person records from the 2022 American Community Survey (ACS) 1-Year
Public Use Microdata Sample (PUMS) for Wyoming (state FIPS 56). Wyoming is
the least-populous U.S. state, making this the smallest state-level PUMS
file — ideal for fast tests and examples.
}
\details{
\strong{Survey design:} Successive difference replication (SDR). Use
\code{as_survey_rep()} with all 80 replicate weights:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{svy <- as_survey_rep(
  acs_pums_wy,
  weights    = pwgtp,
  repweights = starts_with("pwgtp") & !matches("^pwgtp$"),
  type       = "successive-difference"
)
}\if{html}{\out{</div>}}

\strong{Income adjustment:} Income variables (\code{pincp}, \code{wagp}) are in survey-year
dollars. Multiply by \code{adjinc / 1e6} to convert to 2022 inflation-adjusted
dollars before comparing across ACS years.

\strong{Metadata:}
The ACS PUMS source is a plain CSV with no embedded labels. Columns in
\code{acs_pums_wy} carry no \code{"label"}, \code{"labels"}, or \code{"question_preface"}
attributes. Variable descriptions are documented here in \code{?acs_pums_wy} and
in \code{data-raw/README.md}. Use \code{set_variable_labels()} and
\code{set_value_labels()} to attach labels manually before analysis if needed.
}
\examples{
# Wyoming population represented
sum(acs_pums_wy$pwgtp)

# Age distribution
hist(acs_pums_wy$agep, main = "Age distribution, Wyoming 2022",
     xlab = "Age")

# Confirm 80 replicate weights are present
sum(grepl("^pwgtp[0-9]", names(acs_pums_wy)))
}
\keyword{datasets}
