% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-metadata.R
\name{set_field_dates}
\alias{set_field_dates}
\title{Set the Field Dates}
\usage{
set_field_dates(x, field_start, field_end)
}
\arguments{
\item{x}{A survey design object or \code{data.frame}.}

\item{field_start}{\code{Date(1)}, an ISO 8601 \code{character(1)}, or \code{NULL} to
delete the key. Omit the argument to leave the stored value unchanged.}

\item{field_end}{\code{Date(1)}, an ISO 8601 \code{character(1)}, or \code{NULL} to delete
the key. Omit the argument to leave the stored value unchanged.}
}
\value{
The modified object, invisibly.
}
\description{
Sets the \code{field_start} and \code{field_end} dataset-level metadata keys — the
first and last day in the field — on a survey design object or a data frame.
}
\details{
A convenience wrapper for \code{set_dataset_metadata()} that forwards \strong{only the
arguments the call supplies}. The three states of each argument are
distinct:

\describe{
\item{not supplied}{The stored key is left unchanged.}
\item{\code{NULL}}{The stored key is deleted.}
\item{a value}{The key is set, after validation and coercion.}
}

A value is either a \code{Date} scalar or an ISO 8601 string (\code{"YYYY-MM-DD"}),
which is stored as a \code{Date}. A looser string such as \code{"2026/02/10"} or
\code{"2026-2-1"} is rejected, and the error names this function's own argument.

\code{field_start} must not be after \code{field_end}. The comparison uses the
\emph{effective} value of each key: the new value when the call supplies one, no
value when the call supplies \code{NULL}, and the stored value when the call does
not mention the key. So one call can repair a reversed stored pair by
deleting one date and setting the other, and deleting both always succeeds.

This function never touches \code{field_period} — the prose period has its own
setter, \code{\link[=set_field_period]{set_field_period()}}.
}
\examples{
df <- data.frame(id = 1:5, y = c(2, 4, 3, 5, 1), w = rep(1, 5))
df <- set_field_dates(df, "2026-02-10", "2026-03-04")
extract_field_dates(df)

# Supplying one argument leaves the other key unchanged.
df <- set_field_dates(df, field_end = "2026-03-10")
extract_field_dates(df)

# NULL deletes a key.
df <- set_field_dates(df, field_start = NULL)
extract_field_dates(df)

d <- as_survey(df, weights = w)
d <- set_field_dates(d, field_start = as.Date("2026-02-11"))
extract_field_dates(d)
}
\seealso{
Other metadata:
\code{\link[=classify_question_type]{classify_question_type()}},
\code{\link[=extract_data_name]{extract_data_name()}},
\code{\link[=extract_dataset_metadata]{extract_dataset_metadata()}},
\code{\link[=extract_field_dates]{extract_field_dates()}},
\code{\link[=extract_field_period]{extract_field_period()}},
\code{\link[=extract_higher_is]{extract_higher_is()}},
\code{\link[=extract_metadata]{extract_metadata()}},
\code{\link[=extract_missing_codes]{extract_missing_codes()}},
\code{\link[=extract_question_preface]{extract_question_preface()}},
\code{\link[=extract_reverse_coded]{extract_reverse_coded()}},
\code{\link[=extract_sata]{extract_sata()}},
\code{\link[=extract_survey_name]{extract_survey_name()}},
\code{\link[=extract_universe]{extract_universe()}},
\code{\link[=extract_val_labels]{extract_val_labels()}},
\code{\link[=extract_var_label]{extract_var_label()}},
\code{\link[=extract_var_note]{extract_var_note()}},
\code{\link[=extract_vendor]{extract_vendor()}},
\code{\link[=infer_question_prefaces]{infer_question_prefaces()}},
\code{\link[=set_data_name]{set_data_name()}},
\code{\link[=set_dataset_metadata]{set_dataset_metadata()}},
\code{\link[=set_field_period]{set_field_period()}},
\code{\link[=set_higher_is]{set_higher_is()}},
\code{\link[=set_missing_codes]{set_missing_codes()}},
\code{\link[=set_question_preface]{set_question_preface()}},
\code{\link[=set_reverse_coded]{set_reverse_coded()}},
\code{\link[=set_sata]{set_sata()}},
\code{\link[=set_survey_name]{set_survey_name()}},
\code{\link[=set_universe]{set_universe()}},
\code{\link[=set_val_labels]{set_val_labels()}},
\code{\link[=set_var_label]{set_var_label()}},
\code{\link[=set_var_note]{set_var_note()}},
\code{\link[=set_vendor]{set_vendor()}},
\code{\link[=survey_metadata]{survey_metadata()}},
\code{\link[=survey_weighting_history]{survey_weighting_history()}}
}
\concept{metadata}
