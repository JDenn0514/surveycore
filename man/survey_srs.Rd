% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-classes.R
\name{survey_srs}
\alias{survey_srs}
\title{Simple Random Sample Survey Design}
\usage{
survey_srs(
  data = data.frame(),
  metadata = survey_metadata(),
  variables = list(),
  groups = character(0),
  call = NULL
)
}
\arguments{
\item{data}{A \code{data.frame} containing the survey data. Prefer \code{\link[=as_survey]{as_survey()}}
over calling this constructor directly.}

\item{metadata}{A \link{survey_metadata} object. Created automatically by
\code{\link[=as_survey]{as_survey()}}.}

\item{variables}{A named list of design specification. Set automatically by
\code{\link[=as_survey]{as_survey()}}.}

\item{groups}{Set by surveytidy's \code{group_by()}. Always \code{character(0)} in
standalone surveycore use.}

\item{call}{Language object capturing the construction call.}
}
\value{
A \code{survey_srs} object.
}
\description{
A survey design object for equal-probability simple random samples — surveys
where every unit had an equal chance of selection and there is no clustering
or stratification. Create via \code{\link[=as_survey]{as_survey()}} with no \code{ids}, \code{strata}, or
\code{fpc} arguments.
}
\section{When to use this class}{

Use \code{survey_srs} (via \code{as_survey(df)} or \code{as_survey(df, weights = w)} with
no design structure) when:
\itemize{
\item The sample was drawn by simple random sampling (every unit equally likely
to be selected)
\item There are no clusters or strata to specify
\item You want the standard SRS variance formula: \verb{s²/n × (1 - f)} where
\code{f = n/N} is the sampling fraction (0 when population size is unknown)
}

For non-probability samples or post-hoc calibrated weights (raking,
post-stratification, propensity matching), use \code{\link[=as_survey_calibrated]{as_survey_calibrated()}}
instead.

For complex probability samples with known design structure (PSUs, strata),
use \code{\link[=as_survey]{as_survey()}} with \code{ids}, \code{strata}, and/or \code{fpc} — which creates a
\code{survey_taylor} object.
}

\section{Design variables (\verb{@variables})}{

\describe{
\item{\code{weights}}{Character string naming the weight column, or \code{NULL} for
uniform weights (all equal; auto-generated as \code{"..surveycore_wt.."}).}
\item{\code{probs_provided}}{Logical. \code{TRUE} if the user supplied sampling
probabilities rather than weights.}
\item{\code{fpc}}{Character string naming a finite population correction column,
or \code{NULL}. When \code{NULL}, assumes infinite population (\code{f = 0}).}
\item{\code{fpc_type}}{Character string: \code{"population"} when the FPC column
contains population sizes (values > 1), \code{"fraction"} when it contains
sampling fractions (values in (0, 1]), or \code{NULL} when no FPC is
specified.}
\item{\code{ids}}{Always \code{NULL}. SRS has no cluster structure.}
\item{\code{strata}}{Always \code{NULL}. SRS has no stratification.}
\item{\code{nest}}{Always \code{FALSE}.}
}
}

\seealso{
\code{\link[=as_survey_srs]{as_survey_srs()}} to create a \code{survey_srs} object directly;
\code{\link[=as_survey]{as_survey()}} for Taylor series designs.

Other constructors: 
\code{\link{as_survey}()},
\code{\link{as_survey_calibrated}()},
\code{\link{as_survey_replicate}()},
\code{\link{as_survey_srs}()},
\code{\link{as_survey_twophase}()},
\code{\link{survey_calibrated}()},
\code{\link{survey_data}()},
\code{\link{survey_glm}()},
\code{\link{survey_glm_fit}()},
\code{\link{survey_replicate}()},
\code{\link{survey_taylor}()},
\code{\link{survey_twophase}()}
}
\concept{constructors}
