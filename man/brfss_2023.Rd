% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{brfss_2023}
\alias{brfss_2023}
\title{BRFSS 2023: Behavioral Risk Factor Surveillance System}
\format{
A data frame with 10,400 rows and 22 variables:
\describe{
\item{state}{State FIPS code (e.g., \code{1} = Alabama, \code{6} = California).}
\item{psu}{Primary sampling unit. Use as the cluster ID for variance
estimation.}
\item{ststr}{Stratification variable. Use as the stratum for variance
estimation.}
\item{llcpwt}{Final combined landline and cell phone weight. Use for
population-level estimates.}
\item{llcpwt2}{Landline-only weight (for landline stratum analyses).}
\item{dispcode}{Disposition code: \code{1100} = completed interview,
\code{1200} = partial interview.}
\item{sexvar}{Sex: \code{1} = male, \code{2} = female. (Renamed from \code{SEX} in 2022.)}
\item{age80}{Age in years, top-coded at 80 (continuous).}
\item{ageg5yr}{Age group in 5-year intervals (14 categories, 1 = 18–24,
13 = 80+, 14 = don't know/refused/missing).}
\item{race}{Race/ethnicity (5 categories): \code{1} = White only (non-Hispanic),
\code{2} = Black only (non-Hispanic), \code{3} = Other race only (non-Hispanic),
\code{4} = Multiracial (non-Hispanic), \code{5} = Hispanic.}
\item{educag}{Education level (4 groups): \code{1} = Did not graduate high
school, \code{2} = Graduated high school, \code{3} = Attended college/technical
school, \code{4} = Graduated college/technical school.}
\item{incomg1}{Annual household income (8 categories from < $15,000 to
>= $200,000).}
\item{genhlth}{General health: \code{1} = Excellent, \code{2} = Very good,
\code{3} = Good, \code{4} = Fair, \code{5} = Poor. \code{7}/\code{9} = Don't know/refused.}
\item{physhlth}{Number of days physical health was not good in the past 30
days. \code{0}–\code{30} days; \code{88} = none; \code{77}/\code{99} = don't know/refused.}
\item{menthlth}{Number of days mental health was not good in the past 30
days. Same coding as \code{physhlth}.}
\item{poorhlth}{Number of days poor health prevented usual activities in
the past 30 days. Same coding as \code{physhlth}.}
\item{hlthpl1}{Has any health care coverage: \code{1} = yes, \code{2} = no.}
\item{smoke100}{Smoked at least 100 cigarettes in lifetime: \code{1} = yes,
\code{2} = no.}
\item{drnkany6}{Drank any alcoholic beverages in the past 30 days:
\code{1} = yes, \code{2} = no.}
\item{exerany2}{Did any physical activity or exercise (other than regular
job) in the past 30 days: \code{1} = yes, \code{2} = no.}
\item{bmi5cat}{BMI category: \code{1} = Underweight (BMI < 18.5),
\code{2} = Normal weight (18.5–24.9), \code{3} = Overweight (25.0–29.9),
\code{4} = Obese (>= 30.0).}
\item{wtkg3}{Computed weight in kilograms (2 implicit decimal places;
divide by 100 for kg).}
}
}
\source{
Centers for Disease Control and Prevention. 2023 BRFSS Survey Data.
\url{https://www.cdc.gov/brfss/annual_data/annual_2023.html}
}
\usage{
brfss_2023
}
\description{
A stratified random sample from the 2023 Behavioral Risk Factor Surveillance
System (BRFSS), a U.S. telephone survey of health-related behaviors and
conditions among non-institutionalized adults 18 and older. Up to 200
respondents were sampled per state/territory (52 states/territories, ~10,400
rows total) to keep the dataset size manageable.
}
\details{
\strong{Survey design:} Stratified sample with raking (post-stratification)
weights. When creating a survey design object:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{svy <- as_survey(brfss_2023,
  ids     = psu,
  strata  = ststr,
  weights = llcpwt
)
}\if{html}{\out{</div>}}

\strong{Subsampling note:} The full 2023 BRFSS has 433,323 respondents. This
package dataset uses a stratified random sample of up to 200 respondents per
state/territory (set with \code{set.seed(42)}) to reduce file size. Use
\code{data-raw/download-brfss.R} to rebuild from the full source file.

\strong{Variable labels:} Each column retains its original BRFSS variable label as
an R attribute (e.g., \code{attr(brfss_2023$genhlth, "label")} returns
\code{"GENERAL HEALTH"}).
}
\examples{
# Design summary
head(brfss_2023[, c("state", "psu", "ststr", "llcpwt", "genhlth")])

# States represented
length(unique(brfss_2023$state))

# Check variable label
attr(brfss_2023$genhlth, "label")
}
\keyword{datasets}
