% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-quantiles.R
\name{get_quantiles}
\alias{get_quantiles}
\title{Survey-Weighted Quantiles}
\usage{
get_quantiles(
  design,
  x,
  probs = c(0.25, 0.5, 0.75),
  group = NULL,
  variance = "ci",
  conf_level = 0.95,
  n_weighted = FALSE,
  decimals = NULL,
  min_cell_n = 30L,
  na.rm = TRUE,
  label_values = TRUE,
  label_vars = TRUE,
  name_style = "surveycore"
)
}
\arguments{
\item{design}{A survey design object: \code{survey_taylor}, \code{survey_replicate},
\code{survey_twophase}, \code{survey_srs}, or \code{survey_calibrated}.}

\item{x}{<\code{\link[tidyselect:language]{tidy-select}}> A single unquoted numeric
variable name. Must resolve to exactly one numeric column.}

\item{probs}{Numeric vector of probabilities in (0, 1). Default
\code{c(0.25, 0.5, 0.75)} (IQR + median).}

\item{group}{<\code{\link[tidyselect:language]{tidy-select}}> Optional grouping
variable(s). Combined with any grouping set by \code{group_by()}. Default
\code{NULL}.}

\item{variance}{\code{NULL} or a character vector from \code{"se"}, \code{"ci"}, \code{"var"},
\code{"cv"}, \code{"moe"}, \code{"deff"}. Controls which uncertainty columns appear in
the output. CIs use the Woodruff (1952) back-transformation method and
are \strong{not} symmetric around the estimate. \code{"deff"} is always \code{NA} for
quantiles (no closed-form SRS SE). Default \code{"ci"}.}

\item{conf_level}{Numeric scalar in (0, 1). Confidence level for Woodruff
intervals. Default \code{0.95}.}

\item{n_weighted}{Logical. If \code{TRUE}, add an \code{n_weighted} column with the
sum of weights for non-NA observations in each group. Default \code{FALSE}.}

\item{decimals}{Integer or \code{NULL}. If an integer, rounds all numeric output
columns (e.g., \code{estimate}, \code{se}, \code{ci_low}, \code{ci_high}) to this many
decimal places. Default \code{NULL} (no rounding).}

\item{min_cell_n}{Integer. Minimum unweighted cell count before
\code{surveycore_warning_small_cell} fires. Default \code{30L} (AAPOR guidance).}

\item{na.rm}{Logical. If \code{TRUE} (default), \code{NA} values are excluded from
analysis: observations where the analysis variable is \code{NA} are dropped
from calculations, and observations where any group variable is \code{NA} are
excluded from the output. If \code{FALSE}, \code{NA} observations in the analysis
variable are included in calculations, and observations where a group
variable is \code{NA} are collected into their own group row in the output
(appearing after all non-\code{NA} group rows).}

\item{label_values}{Logical. Accepted for API uniformity; has no visible
effect on \code{get_quantiles()} output. Default \code{TRUE}.}

\item{label_vars}{Logical. Accepted for API uniformity; has no visible
effect on \code{get_quantiles()} output. Default \code{TRUE}.}

\item{name_style}{\code{"surveycore"} (default) or \code{"broom"}. When \code{"broom"},
renames \code{se} → \code{std.error}, \code{ci_low} → \code{conf.low},
\code{ci_high} → \code{conf.high}. The \code{estimate} column is unchanged.}
}
\value{
A \code{survey_quantiles} tibble (also inheriting \code{survey_result}).
\itemize{
\item \verb{[group_cols...]} — group variable columns (when active), first.
\item \code{quantile} — probability label: \code{"p25"}, \code{"p50"}, etc.
\item \code{estimate} — weighted quantile estimate.
\item Variance columns (\code{se}, \code{var}, \code{cv}, \code{ci_low}, \code{ci_high}, \code{moe},
\code{deff}) — only those requested via \code{variance}. CIs are Woodruff
intervals and are generally asymmetric around \code{estimate}.
\item \code{n} — unweighted count of non-NA observations used in the estimate.
\item \code{n_weighted} — sum of weights (only when requested).
}
One row per (group combination × quantile probability). The variable name
and \code{probs} vector are stored in \code{meta(result)}.
}
\description{
Compute survey-weighted quantiles (including the median) for a single
numeric variable using the Woodruff (1952) confidence interval method.
Supports optional grouping, domain estimation, and all five survey design
classes.
}
\examples{
d <- as_survey(nhanes_2017, ids = sdmvpsu, weights = wtint2yr,
               strata = sdmvstra, nest = TRUE)

# IQR + median (default)
get_quantiles(d, ridageyr)

# Median only with SE
get_quantiles(d, ridageyr, probs = 0.5, variance = c("ci", "se"))

# Grouped quartiles
get_quantiles(d, ridageyr, group = riagendr)

}
\references{
Woodruff, R. S. (1952). Confidence intervals for medians and other
position measures. \emph{Journal of the American Statistical Association},
\strong{47}(260), 635–646.
}
\seealso{
Other analysis: 
\code{\link{get_corr}()},
\code{\link{get_freqs}()},
\code{\link{get_means}()},
\code{\link{get_ratios}()},
\code{\link{get_totals}()},
\code{\link{meta}()}
}
\concept{analysis}
