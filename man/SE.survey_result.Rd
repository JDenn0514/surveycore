% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-methods-coef-vcov.R
\name{SE.survey_result}
\alias{SE.survey_result}
\title{Extract standard errors from a survey result object}
\usage{
\method{SE}{survey_result}(object, ...)
}
\arguments{
\item{object}{A \code{survey_result} object from any supported \verb{get_*()} function.}

\item{...}{Unused.}
}
\value{
A named numeric vector. Names are identical to \code{names(coef(object))}.
\code{NA} entries propagate from missing \code{se} column or \code{NA} in \code{se}.
}
\description{
Returns a named numeric vector of standard errors from a \code{survey_result}
object. Values are \code{sqrt(diag(vcov(object)))}, ensuring consistency between
\code{SE()} and \code{vcov()}.
}
\note{
\code{vcov.survey_result()} emits \code{surveycore_warning_vcov_diagonal_only}
when \code{p > 1}. This warning is suppressed inside \code{SE.survey_result()} because
the off-diagonal structure is irrelevant when only standard errors are
requested. Call \code{vcov(result)} directly to observe this warning.
}
\examples{
d <- as_survey(
  nhanes_2017,
  ids = sdmvpsu,
  weights = wtint2yr,
  strata = sdmvstra,
  nest = TRUE
)
result <- get_means(d, ridageyr, variance = "se")
SE(result)
}
