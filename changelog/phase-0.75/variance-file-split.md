# refactor(variance): split variance-estimation.R into four engine-specific files

**Date**: 2026-02-24
**Branch**: feature/variance-file-split
**Phase**: Phase 0.75

## Changes

- Split `R/06-variance-estimation.R` into four purpose-specific files:
  `06-variance-taylor.R`, `06-variance-replicate.R`, `06-variance-srs.R`,
  `06-variance-dispatch.R`; delete the original monolithic file
- Split `tests/testthat/test-variance-estimation.R` into four corresponding
  test files; delete the original test file
- Update comment references in `R/07-utils.R` and `R/03-constructors.R` to
  point to the new file names

## Files Modified

- `R/06-variance-taylor.R` — new file: Taylor attribution header,
  `.svy_onestrat/stage/multistage/recvar()`, `.taylor_build_inputs()`,
  `.taylor_mean()`, `.taylor_total()`
- `R/06-variance-replicate.R` — new file: replicate attribution header,
  `.svy_rep_var()`, `.replicate_estimate/mean/total()`
- `R/06-variance-srs.R` — new file: `.srs_mean()`, `.srs_total()`
- `R/06-variance-dispatch.R` — new file: `.validate_estimation_input()`,
  `get_means()`, `get_totals()`
- `R/06-variance-estimation.R` — deleted
- `tests/testthat/test-variance-taylor.R` — new file: Taylor engine tests
  (NHANES oracle, stratified, clustered, FPC, na.rm, fraction FPC, lonely PSU,
  census FPC)
- `tests/testthat/test-variance-replicate.R` — new file: replicate engine
  tests (BRR/JK1 oracles, na.rm paths, `.svy_rep_var()` direct tests)
- `tests/testthat/test-variance-srs.R` — new file: SRS engine tests (oracle
  comparisons, structure checks, edge cases)
- `tests/testthat/test-variance-dispatch.R` — new file: dispatch tests
  (return structure, error paths, trivial correctness, non-survey inputs)
- `tests/testthat/test-variance-estimation.R` — deleted
- `R/07-utils.R` — update comment referencing old file name
- `R/03-constructors.R` — update comment referencing old test file name
- `man/get_means.Rd` — regenerated by devtools::document() (no content change)
- `man/get_totals.Rd` — regenerated by devtools::document() (no content change)
- `plans/implementation-plan-phase-0.75.md` — mark PR 1 complete
