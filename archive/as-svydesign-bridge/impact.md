# Impact — as-svydesign-bridge

## Estimated scope
- Files touched: 6
  - `R/methods-conversion.R` — the dispatch chain, `.as_svydesign_replicate()`, roxygen
  - `plans/error-messages.md` — one row for the existing class, rows for new classes
  - `tests/testthat/test-conversion.R` — new rows for both issues
  - `tests/testthat/_snaps/conversion.md` — snapshots for new user-facing conditions
  - `NEWS.md` — one entry per issue
  - `man/` — regenerated by `devtools::document()`
- Exported functions added/changed: `as_svydesign()` (accepts a fourth class),
  `as_tbl_svy()` (starts accepting `survey_nonprob` on its own). No signature change.
- New dependencies: none
- CRAN-relevant: no DESCRIPTION change, no export change, no new vignette.
  Roxygen and `man/` change.

## Smallness test (criteria: pipeline-simplified/SKILL.md §Smallness criteria)
- Result: full-required
- Rationale: #237's scope note calls for the full path — the change adds a
  supported conversion, decides which variance estimator the converted object
  carries, and needs a warning decision; #198 has an open design decision about
  what an FPC means on a replicate design.
