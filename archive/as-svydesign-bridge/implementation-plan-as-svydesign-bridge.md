# Implementation plan — as-svydesign-bridge

**Status**: DRAFT
**Revision**: 2.0 — rewritten for the narrowed scope, 2026-09-09. Revision 1.x
planned 31 test-spec rows, 13 files and two commits. Decision D-9 took issue
#198 out. This revision plans issue #237 alone: 18 rows, 8 files, one commit.
**Revision**: 2.1 — plan review pass 1 resolved, 2026-09-09. Eight required
findings applied: three narrowed criteria, three new verification tasks, the
changelog fragment. Two edits moved the file count in opposite directions and
should not be confused. The review added `changelog/fix-as-svydesign-bridge.md`
as a ninth file, and it stayed. `NEWS.md` was already in the surface before the
review, and D-10 took it out. Eight files stand.
**Target version**: 1.1.0.9000
**Date**: 2026-09-09
**Branch**: `fix/as-svydesign-bridge`, cut from `develop`, targeting `develop`

This plan covers `spec.md` revision 1.3 and `test-spec.md` revision 1.3. Every
measured figure carries its `measurements.md` R-number. A figure with no
R-number comes from a read of a working tree in this session, and each one names
the file, the line and the tree it was read in.

---

## PR map

- [x] PR 1: `fix/as-svydesign-bridge` — `as_svydesign()` and `as_tbl_svy()`
      accept a `survey_nonprob` design and route it on the shape of its
      weights.

### Budget

| Figure | This PR | Bound |
|---|--:|--:|
| Test-spec rows covered | 18 | 12 |
| Acceptance criteria | 6 | 8 |

The row figure is over the bound. The criteria figure is inside it.

The 18 rows are B-1 to B-7, B-9, B-10, B-11, C-1, C-2, D-1, D-2, E-1, E-2, E-3
and E-4. Sections A and F and row C-3 left with issue #198, and the letters are
not reused. §Acceptance criteria maps every row to a criterion.

Decision D-8 instructed one PR when this work was two issues sharing one
dispatch chain. That reason is gone: D-9 took the second issue out, so no
concurrent PR shares a source file. The instruction stands on its own. The user
instructed one PR twice — at the request, and again when the orchestrator put
the overage to them with the calibration behind the bound. Do not split. Both
figures above are the real ones. The budget ledger,
`plans/pr-budget-calibration.md`, records the real diff after the merge; that
entry is not part of this PR's write surface.

### Pipeline tier

**recommended.**

Three facts put it there:

- The change alters the contract of an exported function. `as_svydesign()`
  gains a fourth input class.
- It adds one condition class to the public register,
  `surveycore_warning_nonprob_srs_conversion`.
- It alters numerical behaviour on one path. A nonprob design's converted
  object now carries the estimator its shape implies, where the call used to
  refuse. Measured (R3): the two shapes answer 0.0200827192 and 0.5630671378,
  and the wrong route on the replicate shape answers 0.5630671378, which is 28
  times too large on that design.

### Files touched — eight

| # | File | Action |
|--:|---|---|
| 1 | `plans/error-messages.md` | One new dated subsection with rows CN-1 and CN-3, plus one Coverage Map line |
| 2 | `R/methods-conversion.R` | The fourth dispatch branch with the CN-3 warning. Roxygen on `as_svydesign()` and on `as_tbl_svy()` |
| 3 | `tests/testthat/test-conversion.R` | New blocks for sections B, C, D and E |
| 4 | `tests/testthat/_snaps/conversion.md` | Appended. Rows B-4 and E-3 |
| 5 | `vignettes/surveycore-vs-survey.Rmd` | One §5 table row and one new subsection, §5.1 |
| 6 | `changelog/fix-as-svydesign-bridge.md` | New file. One fragment for this arc, per task 22 |
| 7 | `man/as_svydesign.Rd` | Regenerated by `devtools::document()` |
| 8 | `man/as_tbl_svy.Rd` | Regenerated by `devtools::document()` |

One PR, so no two concurrent PRs share a file. §Risk and rollback names the one
neighbouring arc and the one file a conflict could reach. It was two files
until D-10 removed `NEWS.md` from the surface.

**`NEWS.md` is not in this write surface.** The user instructed on 2026-09-09
that this work records itself in the changelog fragment alone (D-10). That
also settles a contradiction the plan review found:
`.claude/agents/builder.md:138` and `.claude/agents/shipper.md:195` both forbid
editing `NEWS.md` inside a PR, while five recent PRs did it anyway. This work
does not, so neither contract is strained.

The quality gate is now that `NEWS.md` shows no diff at all.

### The commit

One commit.

```
fix(conversion): route a survey_nonprob design on its weight shape (#237)
```

That subject is 72 characters.

**A note on the scope.** `.claude/rules/github-strategy.md` lists seven scopes —
`classes`, `constructors`, `metadata`, `validators`, `variance`, `analysis`,
`utils` — and `conversion` is not one of them. The repository's own history uses
`conversion` on the three most recent commits to this file: `4a1e396`
(readable in `git status` at the session start), and `4c79a5b` and `1f15d35`
(quoted in `decisions.md` §Event). The rule file lags the practice, the same way
the three entries in `spec.md` §Rule documents that lag the code do. Use
`conversion`. Change no rule file in this PR.

---

## Ordering facts

Read all seven before task 1. Each one fixes the position or the shape of a
task. Every line number below was read in a working tree this session, and each
item names which tree.

1. **The register rows go first.** `.claude/rules/code-style.md` requires the
   `plans/error-messages.md` row before any code uses the class. Two rows:
   CN-1 for the existing `surveycore_error_not_survey_object`, and CN-3 for the
   new warning. The new dated subsection goes after
   `### svydesign-replicate-bridge rows (2026-09-04)`, which is
   `plans/error-messages.md:507` and the file's last subsection in both trees.
   Confirm before writing that no row in the file starts `| CN-`. Read at
   `1f15d35`: zero such rows. Read at develop's tip `40700e3`: zero such rows.
2. **CN-2 does not exist in this work, and the gap is deliberate.** The number
   held the replicate FPC warning, which shipped in the other arc as CB-3 with
   the same class name. Record the skip in the new subsection, the way the
   neighbouring subsection records its own skip from CB-2 to CB-5
   (`plans/error-messages.md:532-534`), so a reader does not hunt for a missing
   row. Do not reuse the ID.
3. **`tests/testthat/_snaps/conversion.md` already exists.** New snapshots
   append and no existing block changes. The file grew during the spec phase:
   41 lines and 4 blocks at `1f15d35`, 84 lines and 8 blocks at `40700e3`. Read
   the file at the base you build on, then append.
4. **`devtools::document()` runs before the commit.** Two `man/` pages ship
   with it: `man/as_svydesign.Rd` and `man/as_tbl_svy.Rd`. Both files exist
   today. Commit them with the source that produced them. Do not edit
   `NAMESPACE` by hand; this change adds and removes no export.
5. **The vignette chunk uses the file's own `has_survey` binding.**
   `vignettes/surveycore-vs-survey.Rmd:19` binds
   `has_survey <- requireNamespace("survey", quietly = TRUE)`, and every chunk
   that needs the package carries `eval=has_survey`. The line is 19 in both
   trees. Do not write a second `requireNamespace()` call.
6. **`test_invariants()` is called once, in the B-1 block, for
   `as_survey_nonprob()`.** Do not call it for any other constructor.
   `tests/testthat/test-conversion.R` already covers `as_survey()` at line 121.
   The file has no `as_survey_nonprob()` call at all, in either tree, so
   section B holds that constructor's first-block slot. At develop's tip the
   file carries a fourth call, at line 2150, and that one builds through
   `as_survey_replicate()` — a different constructor, so it does not fill the
   nonprob slot.
7. **The two reverse-direction `@return` lines must not change.** Identify them
   by their text and their enclosing function, never by a number. Each line
   reads:

   ```
   #' @return A `survey_taylor`, `survey_replicate`, or `survey_twophase` object.
   ```

   There are exactly two such lines in `R/methods-conversion.R`. One sits in
   `from_svydesign()`'s block and one in `from_tbl_svy()`'s. The pair has moved
   four times: 344 and 535 as issue #237 wrote them, 344 and 588 at base
   `a7a52c0`, 364 and 750 at `1f15d35`, and 495 and 881 at `40700e3`.

---

## Tasks

Twenty-five tasks, in execution order.

### The base

1. **Confirm the base before the first edit.** Read §Risk and rollback first:
   `develop` moved four commits past this branch during the spec phase. Fetch,
   then fast-forward the branch onto `origin/develop`. The branch holds no
   unique commit, so the update is a fast-forward and not a rebase. Then run:

   ```bash
   git log --oneline 1f15d35..origin/develop -- R tests vignettes plans NEWS.md
   ```

   Re-verify the four forward-direction citations this change writes against:
   `as_svydesign()` at 63, the dispatch chain at 76-90, the
   `surveycore_error_not_survey_object` site at 88, and
   `.as_svydesign_replicate()` at 131. All four hold at `40700e3`. Two other
   citations moved and this change writes at both: the second
   `surveycore_error_not_survey_object` site, 271 at `1f15d35` and 402 at
   `40700e3`, and the `as_tbl_svy()` `@param x` block, 217-218 at `1f15d35` and
   348-349 at `40700e3`.

### The register

2. Write the two register rows into `plans/error-messages.md`. Append one new
   dated subsection, `### as-svydesign-bridge rows (2026-09-09)`, after the
   `### svydesign-replicate-bridge rows (2026-09-04)` block, at the end of the
   file. Add CN-1 and CN-3 with the text `spec.md` §Conditions gives, in the
   column order every table in the file uses:
   `| # | Function | Condition | Level | Error Class | cli Message Template |`.
   Record the CN-2 skip in the subsection, per ordering fact 2.
   - Before writing, search the file for `| CN-` and confirm zero rows.
   - Before writing, search the file for `nonprob_srs_conversion` and confirm
     zero rows. If the class is already present, stop and raise the collision.
     Add no second row for one class.
   - Do not touch rows CB-1 to CB-5. They belong to the other arc and they are
     already filled.
3. Add one line to the Coverage Map table, after its last row. That row is
   `| \`test-dataset-metadata.R\` | ... |`, at line 404 in both trees. The new
   line is:

   ```
   | `test-conversion.R` | CN-1, CN-3 |
   ```

   No `test-conversion.R` line exists in that table today, in either tree.

### The dispatch branch

4. Write the failing test for row B-1 in `tests/testthat/test-conversion.R`.
   Build Fixture 1's replicate shape inline, per `test-spec.md` §Fixture 1.
   Assert the returned object inherits `svyrep.design` and the call raises no
   condition. Add `test_invariants(d)` here, and here only. Start the block with
   `skip_if_not_installed("survey")`.
5. Write the failing test for row B-3. Build Fixture 1's plain shape. Assert the
   returned object inherits `survey.design2`, and assert the call warns with
   class `surveycore_warning_nonprob_srs_conversion`.
6. Run both blocks. Confirm each fails, and confirm each fails with
   `surveycore_error_not_survey_object` rather than on the fixture. That error
   is issue #237's defect.
7. Add the fourth dispatch branch to `as_svydesign()` in
   `R/methods-conversion.R`. It goes after the `survey_twophase` branch and
   before the `else`. Test `S7::S7_inherits(x, survey_nonprob)`, then route on
   `!is.null(x@variables$repweights)`: non-`NULL` to
   `.as_svydesign_replicate()`, `NULL` to `.as_svydesign_taylor()`. Raise CN-3
   in the `NULL` route, before the call. Write the predicate and the message
   text inline; write no helper. Issue #246 carries the consolidation of all
   eight copies. Write the message with `paste0()`, one fragment per source
   line, matching `R/core-validators.R:172-183`. Carry `class=` on the
   `cli::cli_warn()` call.
8. Verify rows B-1 and B-3 pass. Change no line of `.as_svydesign_taylor()` and
   no line of `.as_svydesign_replicate()`. Both are `spec.md` §Out.
9. Write row B-4, the snapshot of the B-3 warning. It appends to
   `tests/testthat/_snaps/conversion.md`. Accept it with
   `testthat::snapshot_review()` and read the diff. Confirm every existing block
   in that file is unchanged. Never `snapshot_accept()`.
10. Write rows B-2 and B-7. B-2 asserts
    `ncol(survey::weights(converted, "analysis"))` equals 8, and asserts all
    eight names `bw_1` to `bw_8` are present in the converted design's
    variables. B-7 asserts that neither nonprob shape raises a warning of class
    `surveycore_warning_replicate_fpc_dropped`, with
    `expect_no_warning(result <- as_svydesign(d), class = ...)`, once per shape.
    Do not assert the class exists.
11. Verify rows B-2, B-4 and B-7 pass.

### The numbers

12. Write rows B-5 and B-6, the standard-error equality on each shape, at
    tolerance 1e-8. Compute the expectation in the block from
    `get_means(d, y1, variance = "se")$se` on the source design. Do not write
    the literal from R3. B-6 captures the surveycore-side
    `surveycore_warning_nonprob_srs_fallback` from its `get_means()` call.
13. Write rows B-9 and B-10, the default `confint()` equality on each shape, at
    tolerance 1e-6. Each block builds its own fixture and converts it again. No
    block reads an object another block left behind. B-10 captures the same
    surveycore-side fallback warning.
14. Write row B-11. Build Fixture 2 in two steps, per `test-spec.md` §Fixture 2:
    build Fixture 1's plain shape the normal way, then read the frame out, set
    one row's `cal_wt` to 0, and write the frame back into the design's `@data`
    property. Assert the standard-error equality at 1e-8 and assert the returned
    object inherits `survey.design2`. Capture two warnings mechanically and
    assert nothing else about either.
15. Verify rows B-5, B-6, B-9, B-10 and B-11 pass. Add no weight guard: the
    conversion reads no weight value, and both shapes convert with standard
    errors that agree exactly (R12).

### The wrapper, the round trip, the refusal register

16. Write rows C-1 and C-2. Each converts a Fixture 1 shape with
    `as_tbl_svy()` and asserts the returned object inherits `tbl_svy`. C-2 also
    asserts the CN-3 warning. Both blocks start with
    `skip_if_not_installed("survey")` and then
    `skip_if_not_installed("srvyr")`. Write no snapshot: B-4 holds the golden
    copy of that message. Change no line of the `as_tbl_svy()` body.
17. Write rows D-1 and D-2. D-1 asserts
    `S7::S7_inherits(result, survey_replicate)` and
    `expect_false(S7::S7_inherits(result, survey_nonprob))` on
    `from_svydesign()`'s output. Measured (R4): the call returns
    `surveycore::survey_replicate`. D-2 rebuilds the design in its own block and
    asserts
    `expect_no_warning(result <- get_means(...), class = "surveycore_warning_nonprob_srs_fallback")`.
18. Extend row E-1, which exists in the file today as a class-only block, to the
    dual pattern: keep the `expect_error(class = ...)` assertion and add row
    E-3, the snapshot of that message. Write row E-2, the same guard on a
    character string. Write row E-4, which asserts the replicate-shaped nonprob
    design raises no condition of class
    `surveycore_error_not_survey_object`. Change the message text at none of the
    four sites of that class: E-3's snapshot records the current text.
19. Verify rows C-1, C-2, D-1, D-2, E-1, E-2, E-3 and E-4 pass.

### Documentation

20. Write the roxygen. Six edits, all in `R/methods-conversion.R`, with the
    replacement text `spec.md` §Documentation gives:
    - the `as_svydesign()` description at lines 33-36;
    - the `as_svydesign()` `@param x` at line 41;
    - the `as_svydesign()` `@return` at lines 42-46, one sentence inserted;
    - three new `@section` blocks, after `@return` and before `@examples`, in
      the order the spec gives: a non-probability design, degrees of freedom on
      a converted design, a filtered design's domain. They run 17, 17 and 13
      lines, for 47. Section 2 points at
      `vignette("surveycore-vs-survey")`;
    - the `as_tbl_svy()` `@param x`, where the second line is deleted and not
      edited. It says `survey_nonprob` "is not supported and will error", and
      that is false after this change.

    Carry no GitHub issue number on any roxygen line. Then confirm, with a
    line-level diff, that the two reverse-direction `@return` lines are
    unchanged. Find them by their text and their enclosing function, per
    ordering fact 7.
21. Write the vignette passage in `vignettes/surveycore-vs-survey.Rmd` §5, with
    the text `spec.md` §The vignette passage gives. One new row inside the
    existing §5 table, after the `Non-probability design` row at line 624 and
    before the `Manipulation` row at 625. One new subsection, §5.1, after the
    table and before the `---` at 628 that closes the section. The one new
    chunk is labelled `bridge-convert` and carries `eval=has_survey`. The
    passage carries no issue number. Write no `NEWS.md` entry: this work
    records itself in the changelog fragment only, per D-10.

### The changelog fragment

22. Write `changelog/fix-as-svydesign-bridge.md`.

    The repo keeps one fragment per arc in `changelog/`. This work writes the
    fragment and no `NEWS.md` entry, per D-10. Base `40700e3` is itself
    `docs(changelog): backfill fragments for #232 and #238`, and the
    neighbouring arc wrote `changelog/fix-svydesign-replicate-bridge.md` for
    its five PRs.

    **Read `changelog/fix-svydesign-replicate-bridge.md` and follow it, not
    `.claude/skills/changelog-workflow.md`.** That document is stale on two
    counts, both verified: it gives the path as
    `changelog/phase-{X}/{branch-name}.md`, and the last three arcs wrote flat
    files at `changelog/{slug}.md`; and its format is thinner than the one the
    neighbouring arc used.

    The shape to follow: a header naming the branch, status, date, PRs and
    issues, then Summary, Changes, Files Modified and Verification. State the
    measured figures in Verification, with their tolerances.

### The verification diffs

23. Diff every function this change must not touch, and confirm zero hunks in
    each. `spec.md` §Quality gates asks for a line-level diff and names them.
    An earlier revision of this plan instructed the builder not to edit two of
    them and never named the third.

    - `.as_svydesign_taylor()`, `.as_svydesign_replicate()` and
      `.as_svydesign_twophase()`. The third matters most: branch 4 inserts
      immediately after the `survey_twophase` branch, so that is where a stray
      edit is most likely, and no row in sections B to E exercises a twophase
      design.
    - `as_tbl_svy()`'s body. Only its two `@param x` lines change.
    - `from_svydesign()`, `from_tbl_svy()` and the three `.from_svydesign_*()`
      helpers.
    - The two reverse-direction `@return` lines. Find them by their text and
      their enclosing function. That pair has moved four times across five PRs
      of the neighbouring arc, so no number in this plan is trustworthy.

24. Grep the diff for a GitHub issue number on a documentation line. `spec.md`
    §Quality gates supplies the check, and no roxygen line anywhere in the
    package carries one. Search the diff's `R/` lines beginning `#'`, and the
    vignette passage, for `#` followed by two or more digits. Both must return
    nothing.

### The gates

25. Run the gates, then commit.
    - `devtools::document()`. Confirm it runs clean and regenerates
      `man/as_svydesign.Rd` and `man/as_tbl_svy.Rd`. Confirm `NAMESPACE` and
      `DESCRIPTION` are unchanged.
    - `air::format_package()`. Confirm it leaves the touched files alone.
    - The fast suite, for the edit-and-run loop:
      `NOT_CRAN=false Rscript -e "testthat::test_local()"`.
    - The full suite, before the commit: `Rscript -e "devtools::test()"`.
    - `devtools::run_examples()`, `R CMD check --as-cran --no-manual`,
      `pkgdown::build_site()`, and `covr::package_coverage()` with
      `NOT_CRAN=true`.
    - The CRAN cookbook scan, per `r-package-profile.md`. An earlier revision
      omitted it while claiming to satisfy every profile gate.
    - `plans/error-messages.md`: confirm the new dated subsection carries rows
      CN-1 and CN-3, in the file's own column order, and carries no row CN-2.
      AC-5 disclaims this check on the grounds that the gate task makes it, so
      the gate task has to make it.
    - `changelog/fix-as-svydesign-bridge.md`: confirm it exists, is not a stub,
      and carries the header plus Summary, Changes, Files Modified and
      Verification. `.claude/skills/changelog-workflow.md` §Validation rules
      gives the same four checks for the file it describes, and they hold for
      this shape too.
    - `NEWS.md`: confirm it shows no diff at all (D-10).
    - Commit, with the subject
      `fix(conversion): route a survey_nonprob design on its weight shape (#237)`.

---

## Acceptance criteria

Six criteria. Each one is observable before the merge, and each one names the
test-spec rows it satisfies. Together they cite all 18 numbered rows, each
exactly once.

**AC-1 — a `survey_nonprob` design converts on the shape of its weights.** The
replicate shape returns an object inheriting `svyrep.design`, raises no
condition, and keeps all eight replicate columns:
`ncol(survey::weights(converted, "analysis"))` is 8 and the names `bw_1` to
`bw_8` are all present. The plain shape returns an object inheriting
`survey.design2` and raises `surveycore_warning_nonprob_srs_conversion`, and
that message's snapshot passes. Neither shape raises
`surveycore_warning_replicate_fpc_dropped`. The replicate shape raises no
condition of class `surveycore_error_not_survey_object`. Rows B-1, B-2, B-3,
B-4, B-7, E-4.

The plain shape's freedom from `surveycore_error_not_survey_object` is not
asserted directly, and no row asserts it. It follows from B-3: that row
requires the call to return a `survey.design2` and to warn, which the error
would prevent. Stated here so the criterion is not read as wider than the rows
behind it.

**AC-2 — the converted nonprob estimates are surveycore's own.** On both shapes,
and on a plain shape carrying one zero-weight row, the converted standard error
equals `get_means(d, y1, variance = "se")$se` at tolerance 1e-8. On both shapes
the default `confint()` bounds equal `get_means(d, y1, variance = "ci")`'s
`ci_low` and `ci_high` at tolerance 1e-6. Rows B-5, B-6, B-9, B-10, B-11.

**AC-3 — the warning reaches a caller through `as_tbl_svy()`.** Both
`as_tbl_svy()` calls return an object inheriting `tbl_svy`, and the plain
nonprob shape raises `surveycore_warning_nonprob_srs_conversion` through it.
Rows C-1, C-2.

`as_tbl_svy()`'s body is unchanged as well, and that is a diff and not a
behavioural outcome, so it sits in the gate task rather than here. C-1 and C-2
would both pass a body edit that preserved behaviour on their two fixtures.

**AC-4 — the round trip returns a probability design and stops warning.**
`from_svydesign()` on the converted replicate-shaped nonprob design returns a
`survey_replicate` and not a `survey_nonprob`. `get_means()` on that rebuilt
design raises no condition of class
`surveycore_warning_nonprob_srs_fallback`. Rows D-1, D-2.

**AC-5 — the refusal register holds, and the message register records it.**
`as_svydesign()` on a plain `data.frame` and on a character string each raise
`surveycore_error_not_survey_object`, and the message snapshot records the
current text unchanged. Rows E-1, E-2, E-3.

The state of `plans/error-messages.md` is not a criterion here. It is a file
check, no test-spec row reads it, and task 2 already specifies the two rows.
The gate task confirms it. The same goes for the changelog fragment: task 22
writes it and the gate task confirms it exists and is not a stub.

**AC-6 — every profile gate passes.** `devtools::document()` runs clean; the
full `devtools::test()` suite passes; `devtools::run_examples()` passes;
`R CMD check --as-cran` reports 0 errors and 0 warnings, with the two
pre-approved notes only; `pkgdown::build_site()` renders §5.1 and the
`bridge-convert` chunk runs; `covr::package_coverage()` measured with
`NOT_CRAN=true` reads at or above 95%; the CRAN cookbook scan is clean, per
`r-package-profile.md`. `NAMESPACE` and `DESCRIPTION` are unchanged, and
`air::format_package()` leaves every touched file alone. This criterion
satisfies all seven checkboxes of `test-spec.md` §Profile gates, which carries
no numbered rows.

An earlier revision enumerated six of the seven and omitted the cookbook scan,
while claiming to satisfy the section in full.

---

## Risk and rollback

The schema does not require this section. It is here because the base moved
after `spec.md` fixed its citations.

### The base

`develop` moved four commits past this branch during the spec phase. Read from
the git files this session can reach, with no shell:

| What | Value | Where read |
|---|---|---|
| `refs/heads/JDenn0514/as_svydesign-fix` | `1f15d35` | `C:/Users/jdennen/surveycore/.git/refs/heads/JDenn0514/as_svydesign-fix` |
| `refs/remotes/origin/develop` | `40700e3` | `.git/refs/remotes/origin/develop` |
| `refs/heads/develop` | `40700e3` | `.git/refs/heads/develop`, and the main checkout is on it |
| the four commits between them | `8c99271`, `4440de1`, `079fb94`, `40700e3` | `.git/logs/refs/remotes/origin/develop:73-76` |

Two of the four arrived by `fetch ... fast-forward` and two by `update by push`.
This session cannot read their subjects: a commit message is not readable from a
ref file, and no shell was available. Task 1 carries the `git log` command that
reads them.

`decisions.md` §Event records the base as `1f15d35`. That is the state of this
worktree, and every line number `spec.md` and `test-spec.md` cite holds in it.
The branch holds no unique commit, so the update onto `40700e3` is a
fast-forward.

**What moved, measured in the develop checkout at `40700e3`.** Four citations
this change writes against hold: `as_svydesign()` at 63, the dispatch chain at
76-90, the `surveycore_error_not_survey_object` site at 88, and
`.as_svydesign_replicate()` at 131. The `as_svydesign()` roxygen holds too:
description 33, `@param x` 41, `@return` 42. Three citations moved:

| Citation | At `1f15d35` | At `40700e3` |
|---|--:|--:|
| the second `surveycore_error_not_survey_object` site | 271 | 402 |
| the `as_tbl_svy()` `@param x` block | 217-218 | 348-349 |
| the two reverse-direction `@return` lines | 364, 750 | 495, 881 |

Confirm the base before the first edit. If `develop` has moved again, re-verify
the four forward-direction citations first.

### The neighbouring arc has already landed

`spec.md` §Out and `decisions.md` D-9 hand the replicate FPC to PR 4 of the
`svydesign-replicate-bridge` arc, on branch `fix/svydesign-replicate-fpc`. That
work is on `develop` now. Measured in the develop checkout at `40700e3`:

| Evidence | Where |
|---|---|
| `surveycore_warning_replicate_fpc_dropped` is raised | `R/methods-conversion.R:257-279` |
| `surveycore_error_fay_rho_unrecoverable` is raised | `R/methods-conversion.R:215-236` |
| an empty-replicate guard raises `surveycore_error_repweights_empty` | `R/methods-conversion.R:144-159` |
| register rows CB-3 and CB-4 are filled | `plans/error-messages.md:530-531` |
| `_snaps/conversion.md` grew from 4 blocks to 8 | 41 lines at `1f15d35`, 84 at `40700e3` |
| `test-conversion.R` gained a fourth `test_invariants()` call | line 2150, on `as_survey_replicate()` |

Neither `fix/svydesign-replicate-fpc` nor `fix/svydesign-replicate-bridge`
exists as a local or a remote ref any more. Both were deleted after the merge.

Five consequences. Each one changes a task or an assertion.

1. **The conflict this section used to warn about is gone.** Both arcs' register
   subsections are already in the file. This change appends a third.
2. **`.as_svydesign_replicate()` changed, and branch 4a still needs no edit.**
   The helper no longer passes `fpc` or `fpctype` to `survey::svrepdesign()`,
   and the `fpctype` default block `spec.md` cites at lines 135-139 is gone.
   That citation is stale. Branch 4a is unaffected: a nonprob design carries
   `fpc` `NULL` in both shapes (R3), so the drop warning does not fire;
   `as_survey_nonprob()` enforces a minimum of two replicate columns, so the
   empty guard does not fire; and `as_survey_nonprob()` accepts only
   `bootstrap`, `JK1`, `JK2` and `JKn`, so the Fay `rho` recovery does not
   fire.
3. **Row B-7 is now a live assertion.** At `1f15d35` the class
   `surveycore_warning_replicate_fpc_dropped` did not exist, so the row proved
   nothing. On `develop` the class exists and the same helper branch 4a calls
   raises it. B-7 now proves a real absence, which is what `test-spec.md` says
   the row is for.
4. **This PR must still not touch `.as_svydesign_replicate()`.** `spec.md` §Out
   puts it there as "no change of any kind". Nothing in the FPC work changes
   that.
5. **Where a conflict could still reach.** One file:
   `plans/error-messages.md`, where a further arc PR could append another
   subsection. That is an append, so a conflict is one added block and resolves
   by keeping both. `NEWS.md` is no longer a shared surface, because this work
   writes no entry there (D-10). No hunk in `R/methods-conversion.R` is at
   risk: this change writes only the roxygen at 33-46, the dispatch chain at
   76-90, and the `as_tbl_svy()` `@param x` block.

### What a BLOCK costs

One PR, so one fix blocks all eight files. Three tasks are the likely source of
a tester BLOCK.

**1. The two snapshot rows, B-4 and E-3.** A snapshot row fails on formatting,
not on logic, and the failure reads as a defect. Both append to
`tests/testthat/_snaps/conversion.md`, which holds 8 blocks at develop's tip.
Accept each snapshot with `testthat::snapshot_review()` and read the diff.
Never `snapshot_accept()`. Confirm every existing block is unchanged with a
line-level diff and not by eye.

**2. The five rows asserting a measured figure: B-5, B-6, B-11 at 1e-8, and
B-9, B-10 at 1e-6.** Each block must compute its expectation from the fixture,
through `get_means()` on the source design. A literal read off `measurements.md`
carries ten printed digits, which is looser than 1e-8 on a figure of order 0.5.
Three of the five also call `get_means()` on a plain-shaped nonprob design,
which raises `surveycore_warning_nonprob_srs_fallback` — existing behaviour, and
not the subject of any of the three. Capture it with
`expect_warning(sc <- get_means(...), class = "surveycore_warning_nonprob_srs_fallback")`.

**3. Row B-11's fixture.** It builds by writing a frame into `@data` after
construction, which works on `survey_nonprob` and on no other class (R12). A
builder who tries to construct the design with a zero weight gets
`surveycore_error_weights_nonpositive` and reads the row as impossible.
`test-spec.md` §Fixture 2 gives the two-step route.

### Rollback

One PR, one commit, eight files. `git revert` on the merge commit undoes the
whole change. No migration, no data format, no export added or removed, no
`DESCRIPTION` change. `plans/error-messages.md` and the changelog fragment
revert with the rest.
