# Implementation — PR 5 — as-svydesign-domain (docs)

**Branch**: `docs/as-svydesign-domain`
**HEAD**: `eb602c0d93e405c3d5b0c4e2123b1c71eca729c4`
**Tree**: `677a9f3fadf6da9fab20b9bfb42d1c709124a236`
**Base**: `d5dcca4732abf3abc8b7805ee8eb3cd9dde5a3b8` (= `origin/develop`, verified
with `git rev-parse HEAD` before any read or write)

## Write surface

- `R/methods-conversion.R` — modified: roxygen only. The
  `@section A filtered design's domain:` block rewritten, and one sentence
  added to `@return`.
- `man/as_svydesign.Rd` — modified: regenerated by `devtools::document()`,
  never hand-edited.
- `changelog/fix-as-svydesign-domain.md` — created.

`git diff --stat origin/develop...HEAD` names exactly those three files:

```
 R/methods-conversion.R               | 72 ++++++++++++++++++++-------
 changelog/fix-as-svydesign-domain.md | 81 ++++++++++++++++++++++++++++
 man/as_svydesign.Rd                  | 73 ++++++++++++++++++++++-------
 3 files changed, 198 insertions(+), 28 deletions(-)
```

`NAMESPACE`, `NEWS.md`, `DESCRIPTION`, `plans/error-messages.md`,
`vignettes/`, `tests/` and every other `man/*.Rd` are byte-identical to
`origin/develop` — checked with
`git diff --stat origin/develop -- NEWS.md NAMESPACE plans/error-messages.md DESCRIPTION tests/ vignettes/ man/`,
which reports `man/as_svydesign.Rd` alone. The five planning files under
`plans/` stay uncommitted and unstaged; the one commit stages by explicit path.

One commit: `eb602c0`.

## Summary

- The `@section A filtered design's domain:` block now states the nine items
  of `spec.md` §Documentation contract, in order: what the converted object
  represents; how a domain arrives and where it is marked; when the
  restriction applies; that the marker column stays and is an internal marker;
  that an `NA` marker counts as outside the domain; that an empty domain still
  converts silently, with the three per-route outcomes; the two-phase
  zero-weighting and the estimator difference; the round trip, with the
  two-phase exception stated separately; and the `as_tbl_svy()` /
  `srvyr::filter()` note.
- Item 6's outcomes are a three-item roxygen list, after a sentence saying the
  conversion raises no surveycore condition on any route: 0 with a standard
  error of 0 on Taylor and on the non-probability shape naming no replicate
  weights; an error from `survey` at estimation time on the replicate route
  and on the non-probability shape naming replicate weights; `NaN` on
  two-phase.
- Item 7 gives both measured pairs from `spec.md` §Out — 58.458 against 58.067
  filtered, 48.9245 against 49.20304 unfiltered — and names the reason
  (phase-1 weight column against the combined two-phase probability). No issue
  number appears in the user-facing text.
- `@return` gains one sentence, placed directly after the class-per-input
  detail: a filtered input returns an object restricted to the active domain,
  and it names the `A filtered design's domain` section for the per-route
  detail.
- `changelog/fix-as-svydesign-domain.md` covers the whole arc. Its Summary
  opens `Closes #245.` and its first sentence names the change to what
  `as_svydesign()` returns. `Refs #261` carries the two-phase estimator
  difference, which this arc does not close.
- No code changed. The roxygen names no internal helper, shows no `@` property
  access, cites no issue number, and every new line is at or under 80
  characters. The three over-80 lines that `awk` reports in the file are at
  789, 790 and 939 and are pre-existing.

## Structural gate

Read on the final state of the file with `grep`, not by eye. Nothing was
edited to make a count come out.

| Check | Command | Expected | Measured |
|---|---|--:|--:|
| Helper definition | `grep -c "^\.restrict_to_domain <- function" R/methods-conversion.R` | 1 | 1 |
| Call sites | `grep -c "\.restrict_to_domain(converted)" R/methods-conversion.R` | 3 | 3 |
| Outside that file | `grep -rn "restrict_to_domain" R/` minus that file | 0 | 0 |
| Mask expression | `grep -rn "r & !is.na(r)" R/ \| wc -l` | 1 | 1 |
| Presence check | `grep -rn "SURVEYCORE_DOMAIN_COL %in% names(frame)" R/ \| wc -l` | 1 | 1 |
| Two-phase frame branch | `grep -rn 'inherits(converted, "twophase2")' R/ \| wc -l` | 1 | 1 |

The definition takes one argument: line 68 reads
`.restrict_to_domain <- function(converted) {`. The three call sites are at
lines 315, 482 and 528 — Taylor, replicate and two-phase. Every count matches
the carry-forward in `decisions.md` §"PR 4 review PASS". No finding to report.

## Measurements

| Run | Result |
|---|---|
| `devtools::document()`, first run | writes `as_svydesign.Rd` only; no other `man/` file, no `NAMESPACE` |
| `devtools::document()`, second run | `md5sum -c` on `man/as_svydesign.Rd` returns `OK`; `git status` unchanged |
| `NOT_CRAN=true devtools::run_examples()` | exit 0; only the pre-existing AAPOR and nonprob-SRS warnings |
| `air format --check R/methods-conversion.R` | exit 0 |
| Line length in the new roxygen | every line ≤ 80 |

The three logs (`.doc1.log`, `.doc2.log`, `.examples.log`) were deleted before
the commit. The full test suite, `devtools::check()`, `covr` and `pkgdown` were
not run — the orchestrator runs the gates. One R process at a time throughout.

## Task checklist

- [x] 1. Base sha confirmed as `d5dcca4732abf3abc8b7805ee8eb3cd9dde5a3b8`,
  equal to `origin/develop`, before any read or write
- [x] 2. The `@section` block rewritten, found by its tag rather than by the
  plan's stale line numbers. Nine items, in order. Item 6 is prose plus a
  three-item list; item 7 states the difference and cites no issue number;
  item 8 states the two-phase exception separately; item 9 names
  `as_tbl_svy()` and `srvyr::filter()` from inside this section
- [x] 3. One sentence added to `@return`, after the class-per-input detail and
  before the value-label detail
- [x] 4. Nothing else in the roxygen changed. `as_tbl_svy()`'s own block is
  untouched. No code, no comment, no other tag
- [x] 5. `devtools::document()` run twice; the second run left the tree
  unchanged. Only `man/as_svydesign.Rd` changed under `man/`; `NAMESPACE` did
  not change
- [x] 6. `changelog/fix-as-svydesign-domain.md` written in the flat format,
  with `**Branches:**` (five), `**PRs:**` (#266, #267, #268, #269 and this
  PR), `**Issues:** #245`, `**Status:** Complete`, `**Date:** 2026-09-11`,
  `## Summary`, `## Files Modified`, `## Changes` and `## Verification`
- [x] 7. Structural gate read with `grep`; every count as expected, table above
- [x] 8. `run_examples()` run once, redirected to a log, tail read, log deleted
  before the commit
- [x] 9. Committed as `eb602c0` on `docs/as-svydesign-domain`, staged by
  explicit path. This file written

## CRAN compliance

- [x] TRUE/FALSE used throughout — no code changed
- [x] `::` used for external calls — no code changed
- [x] No bare `print()`/`cat()`
- [x] No `@importFrom` added; no `\dontrun{}`; no new example
- [x] `devtools::document()` run
- [x] No new export, so `_pkgdown.yml` needs no entry

## Signals raised

None. No HOLD.

## Notes for tester

- The `@return` sentence names the section with backticks, so Rd renders it as
  `\verb{A filtered design's domain}`. The `\section` title itself carries no
  markup, which is how Rd names a section; there is no cross-reference macro
  for a section inside the same topic.
- `SURVEYCORE_DOMAIN_COL` is named in the section, as `spec.md` item 2
  requires. It is a package constant and not an internal helper or a property,
  so it is inside the code-style rule rather than against it.
- `devtools::run_examples()` emits the AAPOR small-cell and nonprob-SRS
  warnings that clean `develop` already emits. This PR adds no example and
  changes no example, so the warning set is unchanged.
